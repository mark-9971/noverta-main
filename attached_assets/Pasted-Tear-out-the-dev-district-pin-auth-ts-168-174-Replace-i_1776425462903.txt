Tear out the dev district pin (auth.ts:168-174). Replace it with: if tenantDistrictId == null, look up by Clerk user → staff → school → district; if still null, return 403 with a clear message. The "fall back to seeded dev district so the user isn't stranded" comment describes a development convenience that has become a multi-tenant safety hazard. Keep the dev convenience behind an explicit flag like TRELLIS_DEV_FORCE_DISTRICT_ID.
Make invite-staff actually invite. Either (a) call sendEmail with a magic-link / Clerk invitation, or (b) rename the response field to staffCreated and stop showing "Invite sent" toasts in the wizard.
Make sample-data idempotent. If hasSampleData is already true, return the existing counts and a 200, don't re-seed. (This is a one-line guard at the top of the route.)
Tighten validation on the wizard endpoints: reject serviceTypes[].name==="", reject schools[].name==="", reject districtName.trim()==="". Same shape, just if (!x.trim()) return 400.
Default isMakeup: false in the sessions POST schema — or change the zod error path so the first time a new district hits the API directly they don't see [{"code":"invalid_type"...}].
Fix staffTable is not defined in routes/sessions/reports.ts:29 — this is a runtime crash on a dashboard widget, single-line import bug.
Stop auto-creating "Sample Elementary" in the CSV upload path. If the user uploads roster rows for a district that has no schools yet, ask them in the wizard step.
Make /setup show a sign-in prompt when Clerk hasn't resolved a session yet, instead of an empty page. Same for /billing.
Either build the SIS connectors or remove them from the wizard. Today the SIS step looks identical to a real integration and persists credentials, but does no sync. That is the single most likely cause of a churn-on-day-2 conversation. A "CSV only — SIS connectors coming Q3" label is more honest and protects the brand.
Add a separate, persistent "Sandbox District" concept (your suggestion in the prompt). My recommendation: a districts.is_sandbox boolean, a Clerk role sandbox_admin, and a top-level switcher in the user menu so a real customer can flip into a practice copy of their own district to train staff without writing real session logs. This is also where you'd safely run any future onboarding QA without the dev pin needing to exist