Use the actual current codebase as the source of truth.
Do the work, not just a plan.

This is T-V2-03 — W3 Simulator Core.

-------------------------------------

OBJECTIVE

Build the deterministic 9-month simulation engine that generates:
- session_logs
- alerts
- handling-state transitions
- schedule_blocks (including makeup)
- compensatory_obligations (emergent)

This replaces static/backfilled seeding logic with a true event loop.

-------------------------------------

WHY NOW

W1 (platform) and W2 (domain) are complete.

The current seeder still:
- backfills history
- mutates outcomes after the fact
- does not simulate real time

W3 is the core of Seed Overhaul V2.

-------------------------------------

STRICT SCOPE

You are ONLY building the simulator core.

IN SCOPE:
- v2/simulator/ directory
- deterministic day-by-day event loop
- RNG-driven but seeded behavior
- generation of session outcomes over time
- emission of:
  - session_logs
  - alerts
  - handling-state transitions (via existing helpers)
  - schedule_blocks (makeup only, minimal)
  - compensatory_obligations (threshold-based)

OUT OF SCOPE:
- NO UI changes
- NO route changes
- NO demo overlay
- NO refactor of existing seed-sample-data.ts beyond wiring entry point
- NO touching wedge product files
- NO new scheduling system
- NO “smart optimization” logic

-------------------------------------

ARCHITECTURE REQUIREMENTS (CRITICAL)

1. Domain purity (non-negotiable)

v2/domain/* must remain:
- pure
- deterministic
- side-effect free

Simulator must:
- call domain builders
- NOT embed domain logic

2. RNG discipline

Use ONLY:
- v2/platform/rng

Rules:
- seeded by districtId
- deterministic across runs
- no Math.random anywhere

3. Simulation model

- fixed duration: 270 days (9 months)
- iterate day-by-day
- for each day:
  - determine active students/services
  - decide:
    - session occurs vs missed vs late log
    - log delay (0–X days)
    - alert triggers
    - handling-state transitions (some handled, some untouched)

4. Event emission model

Simulator must OUTPUT structured arrays:

- simulatedSessions[]
- simulatedAlerts[]
- simulatedHandlingEvents[]
- simulatedMakeupBlocks[]
- simulatedCompEvents[]

Do NOT directly write to DB yet.

5. No cheating

You must NOT:
- flip completed sessions into missed
- directly seed alerts
- directly seed compensatory totals

Everything must emerge from simulation.

-------------------------------------

DETERMINISM REQUIREMENTS

You must guarantee:

- same districtId → identical output
- output independent of:
  - test order
  - DB auto-increment state

Add:
- at least 1 golden-vector test verifying deterministic output

-------------------------------------

TESTING REQUIREMENTS (STRICT)

DO NOT run full test suite.

Only run:
- new simulator unit tests
- existing seed-v2 tests if needed

Tests must include:

1. Determinism test
- same seed → identical output

2. Volume sanity
- non-zero sessions
- non-zero alerts

3. No cheating invariant
- alerts must correlate with missed sessions
- compensatory must correlate with accumulated shortfall

4. Performance sanity
- simulation completes under reasonable time (no infinite loops)

-------------------------------------

PERFORMANCE RULES (IMPORTANT)

- Do NOT run all tests
- Do NOT run full typecheck repeatedly
- Only validate touched modules

-------------------------------------

REQUIRED FINAL OUTPUT

1. EXECUTIVE VERDICT
2. EXACT FILES CREATED
3. EXACT FILES MODIFIED
4. SIMULATOR ARCHITECTURE SUMMARY
5. TEST RESULTS (counts + types)
6. DETERMINISM PROOF
7. REMAINING GAPS
8. EXACT NEXT TASKS

-------------------------------------

SUCCESS CRITERIA

W3 is COMPLETE when:

- simulator produces deterministic event streams
- no domain logic duplicated
- no “cheating” mutations exist
- tests prove determinism and invariants
- output is ready to be wired into seeder in W4

-------------------------------------

IMPORTANT

If anything in this plan conflicts with actual codebase constraints:
- call it out explicitly
- do not silently workaround it

Do not broaden scope.
Do not optimize prematurely.
Do not rebuild domain logic.

-------------------------------------