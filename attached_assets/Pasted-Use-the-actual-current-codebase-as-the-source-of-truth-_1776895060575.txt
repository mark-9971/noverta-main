Use the actual current codebase as the source of truth.
Do the work, not just a plan.

This is **T-V2-04 follow-up**.
Do NOT start W5 yet.

Important:
- W4 persistence bridge made strong progress, but it is not fully closed.
- The remaining scope is explicitly:
  1. role usage profiles / operator behavior diversity
  2. DB-level integration proof for the orchestrator
  3. architect round 2 and closure
- Keep scope tightly bounded to finishing W4.
- Do NOT broaden into demo overlay, long-tail module expansion, or broader Seed V2 waves.
- Do NOT run broad suites by default.

Before declaring COMPLETE, verify:
1. role usage profiles are actually implemented in the persistence/simulation path in a meaningful way
2. there is at least one real DB-level integration test for the orchestrator
3. architect round 2 passes
4. no cheating layer was introduced
5. tests/build status are clearly stated
6. remaining gaps are honestly listed
7. unrelated pre-existing failures are separated from regressions

Required final output:
1. EXECUTIVE VERDICT
2. EXACT FILES CREATED
3. EXACT FILES MODIFIED
4. EXACT REASONS FOR EACH CHANGE
5. USER-VISIBLE IMPACT
6. ARCHITECTURE / MODEL IMPACT
7. TEST / BUILD STATUS
8. EXACT REMAINING GAPS
9. EXACT NEXT TASKS
10. FINAL ARTIFACT

Also include:
- W4 FOLLOW-UP SCOPE COMPLETION
- ROLE USAGE PROFILE MODEL
- DB INTEGRATION PROOF
- DEMO / RESET IMPACT
- EXACT PRE-EXISTING FAILURES LEFT UNCHANGED

## Objective

Finish **T-V2-04** completely.

The persistence bridge exists, but W4 is not complete until:
- the seeded history reflects believable uneven role behavior
- the DB writer/orchestrator is proven against a real database path
- architect round 2 passes

## Why now

W5 should not start on top of an incomplete persistence bridge.
W4 is the foundation for:
- believable operational history
- wedge/report truth
- demo-readiness overlay later

## Scope

### In scope
1. **Role usage profiles**
Implement the role-behavior diversity still missing from W4.

At minimum define and wire believable historical behavior differences for:
- admin backlog sweeper
- conscientious case manager
- overloaded provider
- sparse-note para
- nearly inactive user

These do not need to be a full W5 showcase system.
They do need to materially affect seeded historical behavior and persisted outputs.

2. **DB-level integration proof**
Add at least one true integration test for the W4 orchestrator/writer against a real test DB path.
It should prove:
- persisted rows land in the correct tables
- alert/source linkage is correct
- cleanup/orphan behavior is safe
- no silent misassociation occurs

3. **Architect round 2**
Run it and address any remaining findings required for W4 closure.

### Out of scope
- no W5 demo overlay
- no broad long-tail expansion
- no route/UI redesign
- no broad simulator changes unless strictly required
- no full repo suite by default

## Testing / verification requirements

Use the smallest possible verification loop.

Required:
1. targeted typecheck
2. targeted W4 tests
3. DB integration test(s)
4. architect round 2
5. broader checks only if required

Do not rerun broad suites repeatedly without a likely-cause statement first.

## COMPLETE / PARTIAL / INCORRECTLY IMPLEMENTED

### COMPLETE
Use COMPLETE only if:
- role usage profiles are meaningfully wired
- DB integration proof exists
- architect round 2 passes
- W4 can honestly be treated as closed

### PARTIAL
Use PARTIAL if:
- only one of the remaining two core gaps lands
- role profiles stay superficial
- DB test is too weak to prove real persistence behavior

### INCORRECTLY IMPLEMENTED
Use INCORRECTLY IMPLEMENTED if:
- the task broadens into W5
- role behavior is hand-waved rather than implemented
- DB proof is replaced with only more pure unit tests
- the final output claims W4 closure without the remaining work truly landing

## Required final output format

Return exactly this structure:

1. EXECUTIVE VERDICT
2. W4 FOLLOW-UP SCOPE COMPLETION
3. EXACT FILES CREATED
4. EXACT FILES MODIFIED
5. EXACT REASONS FOR EACH CHANGE
6. ROLE USAGE PROFILE MODEL
7. DB INTEGRATION PROOF
8. USER-VISIBLE IMPACT
9. DEMO / RESET IMPACT
10. ARCHITECTURE / MODEL IMPACT
11. TEST / BUILD STATUS
12. EXACT REMAINING GAPS
13. EXACT NEXT TASKS
14. FINAL ARTIFACT