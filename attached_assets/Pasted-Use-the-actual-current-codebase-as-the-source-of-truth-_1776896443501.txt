Use the actual current codebase as the source of truth.
Do the work, not just a plan.

This is **T-V2-05 — W5 Demo Readiness Overlay + showcase system + postResetSummary plumbing**.

-------------------------------------

NON-NEGOTIABLE RULES

1. Overlay NEVER mutates core facts
- You must NOT modify:
  - session_logs
  - alerts
  - compensatory_obligations
  - handling history
- Overlay can ONLY:
  - select
  - tag
  - label
  - prioritize
- If you find yourself updating rows → you are doing it wrong.

2. Simulator + persistence remain source of truth
- W3 + W4 outputs are canonical.
- Overlay sits on top, not inside.

3. Keep scope tight
- This is NOT a UI redesign wave
- This is NOT a long-tail module wave
- This is NOT a full reporting pass

-------------------------------------

OBJECTIVE

Make the seeded district **demo-ready immediately after reset** by:

1. Selecting a deterministic set of high-quality “showcase” cases
2. Tagging them for use across wedge surfaces
3. Producing a post-reset summary artifact
4. Making demo flow reliable without corrupting underlying data

-------------------------------------

WHY NOW

You now have:
- deterministic simulator (W3)
- persisted operational history (W4)

But:
- demo still depends on luck
- wedge density is not guaranteed
- role-switch demo is not controlled

This wave fixes that without breaking truth.

-------------------------------------

SCOPE

### IN SCOPE

1. Showcase case system

Create:
- `demo_showcase_cases` table (or equivalent structure)

Each row:
- studentId
- category (one of the fixed set)
- priority
- notes (optional, for internal/demo use)

Categories (must implement ALL):
- at_risk
- scheduled_makeup
- recently_resolved
- provider_overloaded
- evaluation_due
- parent_followup
- high_progress
- chronic_miss

Target:
- 8 primary showcase cases (one per category)
- + fallback buffer (at least 1 per category)

Selection must be:
- deterministic (seeded RNG)
- derived from real simulated/persisted data
- NOT fabricated

---

2. Overlay selection logic

Create overlay module that:
- scans persisted dataset
- picks best candidates per category
- ranks deterministically
- writes showcase tags

Must NOT:
- create fake rows
- adjust metrics
- patch data

---

3. postResetSummary artifact

Extend existing run summary to include:

- total students
- total sessions
- total alerts
- compliance distribution (approx)
- showcase case counts
- example showcase IDs per category

Expose:
- via seed result return
- optionally available to PilotReadinessPanel / demo control

This is for:
- debugging
- demo confidence
- operator visibility

---

4. Minimal wedge surface integration (VERY LIGHT)

Only if trivial:

- ensure Action Center / Today / Compliance views naturally surface showcase cases due to their real state

DO NOT:
- hardcode UI logic for showcase
- add new UI components unless absolutely required

---

### OUT OF SCOPE

- no rewriting reports
- no redesign of Action Center
- no nav changes
- no new dashboards
- no long-tail module seeding
- no “perfect demo flow scripting”

-------------------------------------

ARCHITECTURE REQUIREMENTS

1. Clear boundary

You must create:
- `v2/overlay/` module

Separation:
- domain = pure definitions
- simulator = event generation
- persistence = DB writes
- overlay = selection/tagging ONLY

---

2. Determinism

- same districtId → same showcase selection
- no time-based randomness
- no DB-order dependence

---

3. No coupling leaks

Overlay must NOT:
- reach into UI logic
- duplicate recommendation logic
- bypass handling-state system

Reuse canonical systems where needed:
- wedge primitives
- recommendation taxonomy
- handling-state APIs  [oai_citation:0‡04_architectural_anchors_hot_files.txt](sediment://file_00000000eb7071fda2b98a670bddb3ec)

---

4. No “demo cheating”

Forbidden:
- forcing alerts to exist
- forcing compliance %
- forcing counts

All must emerge from:
simulator → persistence → overlay selection

-------------------------------------

TESTING REQUIREMENTS

DO NOT run full repo tests.

Run:
- overlay unit tests
- minimal integration tests if needed
- existing V2 tests only if touched

Required tests:

1. Deterministic selection
- same seed → same showcase set

2. Category coverage
- all 8 categories populated (or correctly fallbacked)

3. No mutation invariant
- verify underlying tables unchanged by overlay

4. Summary artifact test
- postResetSummary includes expected fields

-------------------------------------

PERFORMANCE RULES

- no broad test runs
- no repeated full typechecks
- only validate touched modules

-------------------------------------

REQUIRED FINAL OUTPUT (STRICT)

Return EXACTLY this structure:

1. EXECUTIVE VERDICT
2. W5 SCOPE COMPLETION
3. EXACT FILES CREATED
4. EXACT FILES MODIFIED
5. EXACT REASONS FOR EACH CHANGE
6. SHOWCASE SELECTION MODEL
7. POST RESET SUMMARY STRUCTURE
8. USER-VISIBLE IMPACT
9. DEMO FLOW IMPACT
10. ARCHITECTURE / MODEL IMPACT
11. TEST / BUILD STATUS
12. EXACT REMAINING GAPS
13. EXACT NEXT TASKS
14. FINAL ARTIFACT

Do NOT skip sections.
Do NOT summarize vaguely.

-------------------------------------

SUCCESS CRITERIA

W5 is COMPLETE when:

- showcase cases exist and are deterministic
- overlay does not mutate core data
- demo density is guaranteed on reset
- postResetSummary gives immediate visibility
- wedge surfaces naturally reflect strong demo cases
- no architectural boundaries are violated

-------------------------------------

IMPORTANT

If anything in this plan conflicts with the actual codebase:
- call it out explicitly
- do NOT silently workaround it

Do not expand scope.
Do not optimize prematurely.
Do not rebuild existing systems.

-------------------------------------