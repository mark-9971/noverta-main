Use the actual current codebase as the source of truth.
Do the work, not just a plan.

This is **T-V2-05 — W5 Demo Readiness Overlay**.

Important:
- Primary objective: implement the bounded Demo Readiness Overlay.
- Secondary objective: fold in the four deferred MEDIUM items from T-V2-04 because they touch the same persistence/overlay boundary.
- Do NOT broaden into UI redesign, long-tail module completion, or cutover work.
- Do NOT mutate underlying seeded facts.
- Do NOT run broad repo-wide tests by default.

Before declaring COMPLETE, verify:
1. the overlay is tag/select/label only and does not mutate underlying seeded facts
2. showcase selection is deterministic
3. postRunSummary is enriched without breaking existing callers
4. the four deferred W4 medium items are either landed or explicitly called out as still open
5. tests/build status are clearly stated
6. remaining gaps are honestly listed
7. unrelated pre-existing failures are separated from regressions

Required final output:
1. EXECUTIVE VERDICT
2. EXACT FILES CREATED
3. EXACT FILES MODIFIED
4. EXACT REASONS FOR EACH CHANGE
5. SHOWCASE SELECTION MODEL
6. NO-MUTATION INVARIANT PROOF
7. POST-RUN SUMMARY STRUCTURE
8. USER-VISIBLE IMPACT
9. DEMO FLOW IMPACT
10. ARCHITECTURE / MODEL IMPACT
11. TEST / BUILD STATUS
12. EXACT REMAINING GAPS
13. EXACT NEXT TASKS
14. FINAL ARTIFACT

## Objective

Make a freshly-seeded V2 district demo-ready immediately after reset by adding a **Demo Readiness Overlay** that selects and labels showcase cases drawn from real persisted simulator output (W3 + W4).

The overlay must never mutate core facts. It may only:
- select
- rank
- tag
- summarize

Also, land the four deferred T-V2-04 medium items because they live in the same persistence/overlay boundary:
- cleanup + insert in a single transaction in runOverlay
- `schedule_blocks.sourceActionItemId` uses canonical `alert:<id>` refs
- active school year lookup is district-scoped
- `chunkedInsert` db handle is threaded through params instead of imported

## Why now

W3 and W4 made the seeded district historically believable.
But demo quality is still not guaranteed after reset.

W5 exists to bridge:
- realistic seeded district truth
and
- deterministic, reliable first-login demo quality

without cheating.

## Done looks like

1. A new `demo_showcase_cases` table (or equivalent persisted overlay structure) exists with:
- districtId
- studentId
- category
- priority
- notes (optional)
- runId
- timestamps

2. The overlay produces:
- **8 primary showcase cases** (one per category)
- **at least 1 fallback per category**

Canonical categories:
- `at_risk`
- `scheduled_makeup`
- `recently_resolved`
- `provider_overloaded`
- `evaluation_due`
- `parent_followup`
- `high_progress`
- `chronic_miss`

3. Selection is fully deterministic:
- same districtId + same runId → byte-identical showcase rows

4. Overlay is no-mutation:
- overlay may create/update only:
  - `demo_showcase_cases`
  - additive post-run summary fields
- it must not alter the persisted W3/W4 fact tables

5. `postRunSummary` is enriched with:
- `complianceDistribution` (approx counts)
- `showcaseCaseCounts`
- `exampleShowcaseIds` (≤ 3 student IDs per category)
- existing fields remain backward-compatible

6. Existing wedge surfaces naturally surface strong showcase cases through real underlying state:
- no hardcoded UI showcase logic
- no new dashboard/nav work

7. The four deferred W4 medium items are landed in this wave.

8. Architect review returns no critical/high findings.

## Scope

### In scope

#### A. Showcase system
Add `demo_showcase_cases` persisted overlay structure and selection logic.

#### B. Overlay module
Replace the placeholder `v2/overlay` implementation with a real deterministic selector/ranker/writer.

#### C. Summary artifact
Extend `postRunSummary` additively.

#### D. W4 medium-item cleanup
Land the four deferred persistence/overlay-adjacent medium items listed above.

#### E. Minimal natural surfacing only
Allow only trivial summary/plumbing exposure if needed.
No custom UI overlay behavior.

### Out of scope
- UI redesign
- new dashboards
- nav changes
- recommendation taxonomy changes
- handling-state API redesign
- long-tail module completeness
- cutover to `SEED_V2=true`
- deletion of V1 seed paths
- PRE-6 / PRE-7

## Implementation constraints

### 1. No mutation of core facts
The overlay must not modify:
- `session_logs`
- `alerts`
- `compensatory_obligations`
- `action_item_handling`
- `action_item_dismissals`
- handling history / related persisted W3/W4 fact tables
- any other persisted simulator fact tables

If you need to touch a table outside `demo_showcase_cases` and additive summary outputs, justify it explicitly.

### 2. Determinism
Use seeded selection/ranking only.
No wall-clock randomness.
No DB-order dependence.

### 3. Reuse boundaries
Respect:
- `v2/domain/*` purity
- `v2/simulator/*` DB-write-free contract
- `v2/persistence/*` as canonical persisted fact layer
- `v2/overlay/*` as selection/tagging/summarization only

### 4. Migration/schema discipline
Add the overlay table using the project’s normal schema/migration workflow.
Do not hardcode a risky migration method in this task unless truly required by the actual repo workflow.

## Testing requirements

Use the smallest possible test loop.

Run only:
- overlay unit tests
- touched persistence/summary tests
- one thin DB integration test
- targeted typecheck for touched packages/modules

Do NOT run the full repo suite by default.

### Required tests

1. **Deterministic selection**
- same seed/run → same showcase rows

2. **Category coverage**
- all 8 categories populate, or fallback is explicitly recorded and surfaced honestly

3. **No-mutation invariant**
- snapshot/hash all overlay-adjacent persisted W3/W4 fact tables before and after overlay
- prove only:
  - `demo_showcase_cases`
  - additive summary outputs
change

4. **Summary shape**
- enriched `postRunSummary` contains the required fields
- existing fields remain intact

5. **DB integration**
- run real seed → persistence → overlay path against test DB
- verify category coverage
- verify no-mutation invariant
- verify idempotent rerun behavior

## Relevant files

Likely touch points include:
- `.local/plans/seed-overhaul-v2.md`
- `lib/db/src/v2/overlay/index.ts`
- `lib/db/src/v2/postRunSummary.ts`
- `lib/db/src/v2/persistence/runOverlay.ts`
- `lib/db/src/v2/persistence/payload.ts`
- `lib/db/src/v2/persistence/mapping.ts`
- `lib/db/src/v2/persistence/roleProfiles.ts`
- `lib/db/src/v2/index.ts`
- `lib/db/src/schema/*`
- targeted `seed-v2` tests

## COMPLETE / PARTIAL / INCORRECTLY IMPLEMENTED

### COMPLETE
Use COMPLETE only if:
- showcase selection is deterministic
- overlay never mutates persisted W3/W4 facts
- 8 categories + fallback behavior are implemented
- postRunSummary is enriched additively
- the four deferred W4 medium items are landed
- architect review returns no critical/high findings

### PARTIAL
Use PARTIAL if:
- overlay exists but coverage/fallback is weak
- no-mutation proof is incomplete
- summary artifact is incomplete
- some W4 deferred items remain open

### INCORRECTLY IMPLEMENTED
Use INCORRECTLY IMPLEMENTED if:
- overlay mutates facts
- fake rows / KPI patching are introduced
- UI-specific showcase logic is added
- broad unrelated work is pulled in

## Required final output format

Return exactly this structure:

1. EXECUTIVE VERDICT
2. W5 SCOPE COMPLETION
3. EXACT FILES CREATED
4. EXACT FILES MODIFIED
5. EXACT REASONS FOR EACH CHANGE
6. SHOWCASE SELECTION MODEL
7. NO-MUTATION INVARIANT PROOF
8. POST-RUN SUMMARY STRUCTURE
9. USER-VISIBLE IMPACT
10. DEMO FLOW IMPACT
11. ARCHITECTURE / MODEL IMPACT
12. TEST / BUILD STATUS
13. EXACT REMAINING GAPS
14. EXACT NEXT TASKS
15. FINAL ARTIFACT