Use the actual current codebase as the source of truth.
Do the work, not just a plan.

This is T-V2-05 — W5 Demo Readiness Overlay.

⸻

Operating constraints (READ FIRST)

* Primary objective: implement the Demo Readiness Overlay (selection + tagging only).
* Secondary objective: land the four deferred MEDIUM items from T-V2-04 (same boundary).
* Do NOT mutate underlying seeded facts.
* Do NOT introduce UI-specific showcase logic.
* Do NOT run full-repo tests — only touched modules + targeted tests.
* Respect architecture boundaries:
    * domain = pure
    * simulator = pure (no DB)
    * persistence = canonical facts
    * overlay = selection + tagging only

Before declaring COMPLETE, verify:

1. Overlay does NOT mutate persisted W3/W4 fact tables
2. Showcase selection is deterministic
3. postRunSummary is enriched without breaking existing shape
4. All four deferred W4 medium items are handled (or explicitly listed as gaps)
5. Tests + typecheck status are clearly reported
6. Remaining gaps are explicitly listed
7. Pre-existing failures are separated from regressions

⸻

Objective

Make a freshly-seeded V2 district demo-ready immediately after reset by adding a Demo Readiness Overlay that selects and labels showcase cases from real simulator output (W3 + W4).

Overlay must:

* select
* rank
* tag
* summarize

Overlay must NOT:

* mutate facts
* fabricate data
* patch KPIs

⸻

Done looks like

1. A new demo_showcase_cases table exists
    * Created via the repo’s standard schema/migration workflow
    * Fields:
        * districtId
        * studentId
        * category
        * priority
        * notes (optional)
        * runId
        * timestamps
2. Overlay produces:
    * ≥ 8 primary showcase cases (1 per category)
    * ≥ 1 fallback per category

Categories:

* at_risk
* scheduled_makeup
* recently_resolved
* provider_overloaded
* evaluation_due
* parent_followup
* high_progress
* chronic_miss

3. Deterministic selection:

* same (districtId, runId) → identical output

4. No-mutation invariant
    Overlay must not modify persisted W3/W4 fact tables.

Invariant test must snapshot ALL relevant fact tables, including at minimum:

* session_logs
* alerts
* compensatory_obligations
* action_item_handling
* action_item_dismissals
* iep_events

And prove that ONLY:

* demo_showcase_cases
* additive postRunSummary fields

change.

5. postRunSummary enriched (additive only):

* complianceDistribution
* showcaseCaseCounts
* exampleShowcaseIds (≤3 per category)

6. W4 deferred MEDIUM items are landed:

* cleanup + insert wrapped in a single transaction
* schedule_blocks.sourceActionItemId uses canonical alert:
* active school year lookup is district-scoped
* chunkedInsert DB handle is passed via params (not imported)

7. No UI changes:

* No new components
* No nav changes
* Surfaces should naturally reflect seeded data
* Verify natural surfacing where possible, but DO NOT add UI logic

8. Architect review returns no critical/high issues

⸻

Scope

In scope

* demo_showcase_cases schema + usage
* overlay selection logic
* postRunSummary enrichment
* W4 medium cleanup items

Out of scope

* UI redesign
* dashboards / nav changes
* long-tail module expansion
* recommendation system changes
* SEED_V2 cutover
* deleting V1 seeders
* PRE-6 / PRE-7

⸻

Implementation steps

1. Schema

* Add demo_showcase_cases
* Register in schema barrel
* Apply via standard migration workflow
* Verify reseed + FK behavior

⸻

2. Overlay module

Replace placeholder:
lib/db/src/v2/overlay/index.ts

With:

* deterministic selector
* per-category candidate filtering
* ranking via seeded PRNG (districtId + runId)
* primary + fallback assignment
* writes ONLY to demo_showcase_cases

⸻

3. Seed pipeline wiring

* Run overlay AFTER persistence (W4)
* BEFORE summary
* Cleanup + insert occurs in same transaction

⸻

4. Fold W4 deferred items

In persistence layer:

* transaction wrap
* canonical alert: references
* district-scoped school year lookup
* chunkedInsert refactor

⸻

5. Summary plumbing

Extend postRunSummary:

* additive fields only
* maintain backward compatibility

⸻

6. Tests

Run ONLY:

* overlay unit tests
* touched persistence tests
* 1 DB integration test
* typecheck for touched modules

Required tests:

1. Determinism
2. Category coverage + fallback
3. No-mutation invariant (hash/digest-based)
4. Summary shape
5. DB integration:
    * seed → persist → overlay
    * coverage verified
    * invariant verified
    * idempotent rerun

⸻

Required final output format (STRICT)

Return EXACTLY these sections:

1. EXECUTIVE VERDICT
2. W5 SCOPE COMPLETION
3. EXACT FILES CREATED
4. EXACT FILES MODIFIED
5. EXACT REASONS FOR EACH CHANGE
6. SHOWCASE SELECTION MODEL
7. NO-MUTATION INVARIANT PROOF
8. POST-RUN SUMMARY STRUCTURE
9. USER-VISIBLE IMPACT
10. DEMO FLOW IMPACT
11. ARCHITECTURE / MODEL IMPACT
12. TEST / BUILD STATUS
13. EXACT REMAINING GAPS
14. EXACT NEXT TASKS
15. FINAL ARTIFACT

Rules:

* No missing sections
* No vague summaries
* Must include file paths + counts
* Must distinguish regressions vs pre-existing issues

⸻

COMPLETE / PARTIAL / INCORRECTLY IMPLEMENTED

COMPLETE

* deterministic overlay
* no mutation proven
* all 8 categories + fallbacks
* summary enriched
* W4 medium items landed
* no critical/high issues

PARTIAL

* overlay exists but incomplete coverage / invariant

INCORRECTLY IMPLEMENTED

* mutates facts
* fabricates data
* adds UI logic
* broad scope creep

⸻

Relevant files

.local/plans/seed-overhaul-v2.md
lib/db/src/v2/overlay/index.ts
lib/db/src/v2/postRunSummary.ts
lib/db/src/v2/persistence/runOverlay.ts
lib/db/src/v2/persistence/payload.ts
lib/db/src/v2/persistence/mapping.ts
lib/db/src/v2/persistence/roleProfiles.ts
lib/db/src/v2/index.ts
lib/db/src/v2/platform
lib/db/src/v2/simulator
lib/db/src/v2/scenarios
lib/db/src/schema
artifacts/api-server/tests/seed-v2/persistence-overlay-integration.test.ts
artifacts/api-server/tests/seed-v2/persistence-payload.test.ts
artifacts/api-server/tests/seed-v2/post-run-summary.test.ts