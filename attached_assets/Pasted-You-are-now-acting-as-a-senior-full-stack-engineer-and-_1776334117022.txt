You are now acting as a senior full-stack engineer and demo-readiness fixer for this app.

You have already completed an audit and identified the highest-priority demo / pilot risks. Your job now is to execute the fixes carefully, verify them, and leave the app in a safe state for a live demo using my Clerk dev account.

Goal:
Raise demo readiness as much as possible, fast, without destabilizing working features.

Important operating rules:
- Do not do a broad rewrite.
- Do not refactor unrelated code.
- Do not change working core flows unless required for the fixes below.
- Prefer minimal, safe, reversible changes.
- Verify every fix after implementing it.
- If something is config/data-related, fix seed data or DB records before touching app logic.
- If a feature is incomplete and risky for demo, hide it or make the empty state intentional rather than leaving it awkward.
- Be explicit and surgical.

Context from prior audit:
- App has real working flows and should be preserved.
- Biggest issues are mostly demo-data and a few user-linking / gating issues, not core architecture.
- Highest-priority risks:
  1. Missing 2025-2026 school year
  2. No staff record linked to my Clerk admin user
  3. 251 open restraint incidents
  4. Empty Agencies / Contract Utilization areas
  5. Potential subscription/tier gating issues for demo district

Your mission:
Implement the critical demo fixes first, verify them, then produce a concise completion report.

Execute in this order:

## Phase 1 — Safe database/data fixes first

### 1. Add current school year
- Check whether a school year covering Sep 2025 through Aug 2026 already exists for district_id = 2
- If missing, create it
- Make sure there is no logic conflict with current or active school year lookup

Success condition:
- Date-based pages reference the 2025-2026 year correctly
- No duplicate conflicting “current school year” behavior

### 2. Link my Clerk admin user to a real staff record
- Inspect how current-user staff lookup works
- Find the Clerk user id / metadata used at runtime in dev
- Create or update a staff record so the logged-in Clerk dev account maps cleanly to a staff profile
- Use role = admin unless the app requires a different internal role for staff-based lookups
- Preserve district scoping

Success condition:
- “My Caseload” no longer fails or shows a broken empty result due solely to missing user linkage
- Any “current staff member” lookup works for the demo account

### 3. Clean up restraint incidents for demo realism
- Do not delete data
- Bulk-close most old incidents
- Keep a believable small number open, preferably the most recent 5–10
- Add resolution dates / closed statuses consistently

Success condition:
- Dashboard / needs-attention views look realistic
- Protective Measures page still demonstrates functionality

### 4. Seed Agencies + Contract Utilization demo data
- Create 3–5 believable agency records
- Create matching contracts with reasonable dates, statuses, utilization values, and service relationships if the schema supports them
- Make the page feel real, not stuffed with junk
- Use coherent names and realistic sample values

Success condition:
- Agencies page is populated
- Contract Utilization page is populated
- No errors or broken joins

### 5. Verify feature/tier access for demo district
- Inspect subscription / feature-gate logic
- Ensure district_id = 2 has a subscription/tier that unlocks all features needed for demo
- Do not weaken auth globally; only make sure the demo district is properly configured

Success condition:
- Executive Dashboard, District Overview, FBA/BIP, Evaluations, and other intended demo pages are accessible for the admin user
- No misleading upgrade locks during demo

---

## Phase 2 — Hide or soften awkward demo risks

### 6. Audit sidebar / nav items that are visibly unfinished
Review items marked “Coming Soon” or incomplete:
- Attendance
- ABA Graphing
- Caseload Balancing
- Budget
- Credentialing
- Supervision Log
- Parent Portal
- Documents
- Translation

Rules:
- If already labeled clearly as “Coming Soon” and harmless, leave them
- If clickable and awkward, either:
  - disable navigation cleanly
  - add a polished placeholder page
  - or hide them behind a feature flag for demo mode
- Choose the least risky option

Success condition:
- No embarrassing dead-end clicks in demo navigation

### 7. Review My Caseload behavior after staff-link fix
- If it still doesn’t make sense for an admin user, make the empty/admin state intentional and polished
- Example: show a friendly explanatory state like “Admin users do not carry a direct service caseload” if that matches product intent
- Do not leave a broken empty table that looks accidental

Success condition:
- My Caseload either works properly or fails gracefully in a polished, intentional way

### 8. Review SIS Settings and Billing for demo safety
- If these areas are incomplete or external-service-dependent, make the UI honest and safe
- Replace ugly errors / unsupported actions with clear informational states
- If SFTP attendance import is not supported, show a clean “Not yet available in this environment” state
- If Billing depends on Stripe setup not present in demo, show a stable configuration/demo message rather than broken behavior

Success condition:
- Visiting these pages no longer creates confusion or obvious incompleteness

---

## Phase 3 — Verify critical demo path end to end

Run a real click-through test for this path:

1. Sign in through Clerk
2. Dashboard
3. Students list
4. Student detail
5. Student IEP page
6. IEP Builder
7. Sessions
8. Compliance
9. Behavior Assessment / FBA-BIP
10. Supervision
11. Schedule
12. Protective Measures
13. Reports / Exports
14. Alerts
15. Audit Log
16. Sign out

For each step:
- verify page loads
- verify data is present
- verify there are no blocking console errors
- verify there are no obvious blank or broken states
- verify the action is demo-safe

If a step fails:
- fix it if the fix is small and low-risk
- otherwise clearly mark it as “avoid in demo” and explain why

---

## Deliverables

When done, produce:

### A. What you changed
For each change include:
- exact issue fixed
- files