/**
 * Seed Overhaul V2 — Domain foundation boundary (W1 placeholder).
 *
 * Districts, schools, staff, students, IEP docs, goals, service
 * requirements, schedule blocks, accommodations — i.e. T0 facts — will
 * be decomposed into modules here in W2 (see
 * .local/plans/seed-overhaul-v2.md §11.4).
 *
 * W1 does NOT move any domain code. The existing `seedSampleDataForDistrict`
 * still owns every domain insert. This file is the boundary marker for
 * that future move.
 */

export const DOMAIN_LAYER_VERSION = "w1-placeholder";
