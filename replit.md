# Trellis — SPED Operations, Service Delivery & Compliance Platform

## Overview

Trellis is a SPED service-delivery and compliance-risk platform for Massachusetts school districts (603 CMR 28.00/46.00). The product positioning: **"Trellis helps SPED teams track whether mandated services are actually being delivered, flag compliance gaps early, and reduce compensatory risk."**

The platform is NOT a full SIS replacement — it syncs with SIS systems but its job is the IEP-delivery side: service minutes, missed sessions, compensatory exposure, and the documentation needed to prove delivery. Marketing language must avoid "AI-powered" framing unless the underlying feature actually uses an AI model (the IEP-PDF extraction in `imports/iepDocuments.ts` does — it uses OpenAI gpt-5.2 — and is the only surface where "AI" is mentioned).

**Standing tagline (used in nav subtitles, sign-in/up, sign-document, pricing header/footer):** "Service-minute compliance for SPED."

## Pilot Feature Organization

See `docs/pilot/feature-status.md` for full module classification. Navigation has been reorganized around the pilot wedge:

- **CORE (build now):** Dashboard, Alerts, Students, Sessions, Schedule, Staff, Reports, Compliance, Document Workflow, Parent Comms, Settings
- **DEMO (sell harder):** Compliance Dashboard, Progress Reports, IEP Builder, IEP Meetings, Protective Measures, Executive Dashboard, Clinical tools
- **RESTORED (in primary nav):** IEP Search, Evaluations, IEP Calendar (Students); Staff Calendar, Coverage (Service Delivery); Compensatory Services, State Reports (Compliance); Contract Utilization, Caseload Balancing (District); Agencies (People); Data Import, Billing (Admin)
- **DEMOTED to "More tools" (collapsed-by-default group at the bottom of the sidebar):** Transition Planning, IEP Suggestions, Supervision, Resource Management, Analytics. Routes and page files are intact; FeatureGate still controls per-role/tier visibility.
- **PROMOTED into Compliance group (out of Admin):** Compliance Risk Report, Weekly Compliance Summary — co-located with the rest of the wedge story.

**Codebase Refactoring (April 2026):** Major refactoring initiative completed. All monolithic files (1000-3000 lines) have been broken into focused sub-module directories:

Frontend pages refactored into sub-component directories (state/hooks in index.tsx, sub-components receive props):
- `student-detail/` (7 sub-components), `student-iep/` (9 sub-components), `protective-measures/` (7 sub-components)
- `sessions/`, `para-my-day/`, `evaluations/`, `iep-builder/`, `document-workflow/`, `transitions/`, `iep-meetings/`
- `coverage/`, `progress-reports/`, `program-data/`, `analytics/`, `behavior-assessment/`, `reports/`
- `schedule/`, `parent-communication/`, `staff-calendar/`, `caseload-balancing/`, `setup/`, `medicaid-billing/`
- `supervision/`, `resource-management/`, `dashboard/`, `compensatory-finance/`, `compensatory-services/`
- Components: `quick-log-sheet/`, `bip-management/`, `program-builder/` (TemplateManager split)

**Pilot Admin Home (`PilotAdminHome.tsx`):** First-impression landing page for admin/coordinator at `/`. Replaces the dense legacy admin dashboard (8+ sections) with a focused page that answers four questions for a SPED director: (1) Are we compliant? — large compliance %, status band (on track / watch / at risk), and out/at-risk/on-track student counts from `/api/reports/compliance-risk-report`. (2) Where are we at risk? — top 6 students aggregated from `needsAttention` (deduped to worst service per student), each linking to the student detail page. (3) What needs attention this week? — `urgentFlags` and `providersWithMissedThisWeek` from `/api/reports/weekly-compliance-summary`, with explicit error and loading states (no false "all clear" if the fetch fails). (4) What should I do next? — derived numbered action list (incomplete onboarding → setup; flagged students → risk report; urgent flags → weekly summary; missed sessions → /sessions; combined exposure → /cost-avoidance; always: share weekly summary). Reuses `<CostRiskPanel />` for exposure context. Standing tagline "Service-minute compliance for SPED" appears in the header. The legacy rich admin dashboard is preserved at `/?view=full` (toggled via "View full district dashboard" link); `dashboard/index.tsx` was refactored so the role/view dispatch happens with stable hook order (`Dashboard` only calls `useRole` + `useLocation`, then renders either `<PilotAdminHome>` or the new `<DashboardFull>` component that owns all the data hooks).

**Compliance Risk Report (Killer Report):** Meeting-ready compliance report at `/compliance-risk-report` with executive summary cards (students, compliance %, shortfall, $ exposure), "Needs Attention" section for at-risk/out-of-compliance students, full student detail table, and provider delivery summary. Supports school filtering, print-to-PDF (landscape letter), and CSV export. Backend: `reportExports/complianceRiskReport.ts`, Frontend: `pages/compliance-risk-report.tsx`. Uses `computeAllActiveMinuteProgress` + rate resolution for $ exposure calculations. District-scoped, role-gated (admin/coordinator/case_manager).

**Compliance Dashboard Redesign:** The `/compliance` page (Service Minutes tab) was redesigned around the core SPED workflow: session logging → service delivery tracking → compliance gap detection → compensatory exposure. Layout: 5 KPI cards (Required / Delivered / Shortfall / At-Risk Students / $ Exposure) → "Needs Attention" urgent table (top 10 worst cases from risk report) → Compliance by Service Type chart + Provider Delivery bars → filterable student requirements table. Data comes from both `useListMinuteProgress` (granular per-requirement) and the compliance-risk-report API (aggregated summary/needs-attention/provider). Graceful fallback: if the report endpoint returns 403 for non-admin roles, an amber banner explains partial data and the page degrades to chart + table only. Checklist and Timeline tabs preserved unchanged.

**Weekly SPED Compliance Summary:** Meeting-ready weekly report at `/weekly-compliance-summary`. Backend: `reportExports/weeklyComplianceSummary.ts` with JSON, CSV, and PDF endpoints. Includes: executive summary (required/delivered/shortfall/compliance rate/$ exposure), urgent flags (auto-generated from risk counts + missed sessions + exposure thresholds + declining trends), top 25 student shortfalls sorted by severity, providers with missed sessions this week, full provider delivery summary, and 8-week delivery trend table. Three export modes: (1) Print — opens browser print dialog with landscape HTML, (2) CSV — downloads student shortfall data, (3) PDF — server-generated PDFKit document with district branding placeholder, KPI stat boxes, urgent flags box, all tables with height-aware pagination, striped rows, FERPA footer on every page. Data computation shared via `computeReportData()` function used by all three endpoints. School-filterable. District-scoped, role-gated (admin/coordinator/case_manager). Linked from compliance dashboard header and admin nav.

Backend routes refactored into sub-module directories (index.ts mounts sub-routers):
- `reportExports/`, `protectiveMeasures/`, `iep/`, `students/`, `programData/`, `dashboard/`, `iepMeetings/`, `sessions/`
- `fba/`, `reports/`, `schedules/`, `analytics/`, `agencies/`
- `documentWorkflow/`, `imports/`, `parentMessages/`, `medicaidBilling/`, `compensatoryFinance/`
- `iepBuilder/`, `stateReporting/`, `parentCommunication/`

**Empty States Audit (First-Run Experience):** All major pages now have rich, SPED-specific empty states for new districts with little data. Enhanced `EmptyState` component (`components/ui/empty-state.tsx`) supports ReactNode children with helper sub-components: `EmptyStateStep` (numbered steps), `EmptyStateHeading` (section headers), `EmptyStateDetail` (explanatory paragraphs). Pages covered: Students (SIS import + manual entry guidance), Sessions (IEP mandate tracking context), Compliance Dashboard (4-step data prerequisites), Staff/Providers (all 3 tabs: Clinicians, Paras, Case Managers), Protective Measures (603 CMR 46.00 context), Reports (Executive Summary, Compliance Risk Report, Weekly Summary), IEP Goals & Progress Reports (context-aware based on existing targets). Each empty state explains what the page does, why it matters for Massachusetts SPED compliance, and gives actionable next steps.

**Demo Mode (prospect-safe):** Districts have an `is_demo` boolean column (`lib/db/src/schema/districts.ts`). The seeded "MetroWest Collaborative" district is flagged `is_demo=true`. When the active scope is a demo district, an amber "Demo data" banner renders at the top of every authenticated page (`artifacts/trellis/src/components/DemoBanner.tsx`, mounted in `AppLayout` above `<main>`). Logic for "active demo district": (a) selected district matches a demo district, or (b) no district/school selected AND the only district in the DB is a demo district. Selecting a specific school clears the banner (because schools span both demo and real). The destructive demo seeder (`lib/db/src/seed-demo-district.ts`) now refuses to run if any non-demo district exists in the database, unless `ALLOW_DEMO_SEED_RESET=1` is set — protecting pilot/production data from accidental wipes. The districts list endpoint (`GET /api/districts`) now returns `isDemo` per row.

**Demo District Dataset Generator:** Self-contained seeder at `lib/db/src/seed-demo-district.ts` (runner: `lib/db/run-seed-demo.ts`, run with `cd lib/db && npx tsx run-seed-demo.ts`). Creates "MetroWest Collaborative" — a realistic small-to-mid Massachusetts district (Framingham, MA) with 42 students across 3 schools (Elementary, Middle, High), 21 staff (BCBAs, SLPs, OTs, PT, counselors, paras, RBTs, case managers, admin), 7 service types, 107 service requirements, 42 IEP documents with PLAAFP, 194 IEP goals, ~5000 session logs (completed + missed with clinical notes), 196 conflict-free schedule blocks, staff assignments, alerts, compensatory obligations, behavior & program targets. Six intentionally-modeled student scenarios: healthy (~40%), shortfall (~24%), improving (~12%), compensatory risk (~10%), urgent (~7%), new enrollment (~5%). Data uses Massachusetts disability category distribution, MA school calendar with holidays/breaks, realistic IEP Grid B minute ranges, balanced provider caseloads, and demographically diverse names. Script truncates all related tables with CASCADE before seeding (destructive — wipes existing data).

**Data Health Check (Pre-Pilot Readiness):** Admin-facing diagnostic page at `/data-health` that runs 12 automated checks against the district's imported data to verify it's usable before a pilot starts. Backend: `GET /api/data-health` (admin/coordinator only, district-scoped via schools → students/staff join, fails closed on missing district). Checks run: (1) Students missing service requirements [critical], (2) Students missing case manager [warning], (3) Students not assigned to a school [warning], (4) Service requirements without assigned provider [critical], (5) Service requirements with no minute target [critical], (6) Providers with no student assignments or schedule [warning], (7) Staff without email addresses [warning], (8) Possible duplicate students [warning], (9) Possible duplicate staff [warning], (10) Students with requirements but no schedule blocks [warning], (11) Students with no IEP goals [warning], (12) Students with incomplete demographics [info]. Frontend: `pages/data-health.tsx` with overall readiness status (Ready/Needs Attention/Not Ready), summary stats (students/staff/service reqs/schedule blocks), critical/warning/passed counters, category-grouped expandable check cards with progress bars, per-item detail lists with links to student profiles, and actionable next-steps section. Nav: Admin section, "Data Health Check" with ShieldCheck icon, between Data Import and Billing.

**CSV Import Flow Upgrade (District Onboarding):** End-to-end upgrade of the data import system for small district self-onboarding. Backend: New staff import route (`imports/staff.ts`) with role normalization (slp/ot/pt/bcba/para + common title aliases like "Speech-Language Pathologist"), school matching, email/name duplicate detection, `duplicateHandling` param (skip/update). New pre-import validation endpoint (`POST /api/imports/validate`) returns per-row valid/warning/error status with messages, auto-detected column mappings, and unmappedRequired flags — no data committed. Enhanced student import with DOB, disability_category, school matching, case_manager matching, parent fields, medicaid_id, and duplicate handling. Templates upgraded with 3-5 sample rows, `#` comment instruction lines (stripped by both frontend preview and backend parser), and a staff template. Shared parser (`parseCsvRows`) auto-detects TSV vs CSV, strips comment lines. Frontend (`import-data.tsx`): Complete rewrite with 6 import types (Students, Staff/Providers, IEP Documents, Service Requirements, Sessions, Goals & Progress Data), multi-step flow (upload → validate → import → results), column mapping display, per-row validation with filter/pagination, duplicate handling toggle, breadcrumb navigation, per-type guidance sidebar with prerequisites/tips, and improved template downloads. Import types have badges ("New" for Staff, "AI" for IEP Documents).

**Pitch Deck:** 14-slide investor/district/SIS partner pitch deck at `artifacts/trellis-pitch` with slides covering problem, why now, solution, product depth, platform demo, market opportunity, competitive landscape, business model, traction & roadmap, implementation & pilot, SIS partnership, team approach, and closing CTA.

**Demo Deck:** 25-slide demo/walkthrough deck at `artifacts/trellis-demo` (previewPath `/trellis-demo`, port 20180) explaining what Trellis does, why it matters, and the market opportunity. Includes real product screenshots (dashboard, student list, student detail, compliance, alerts, sessions) and AI-generated imagery. Uses Source Serif 4 + DM Sans fonts with emerald green (#059669) palette. Shareable to anyone without app access. Covers: title, what is Trellis, the problem, problem stats, product walkthrough (6 screenshots), features, differentiators, competitor comparison, TAM/market, MA beachhead, pricing, unit economics, why now, roadmap, founder story, and closing.

The brand uses an "Ink & Air" theme with pure white/near-white backgrounds, near-black typography, neutral grays, and emerald green accents (primary: HSL 160 84% 39%), creating a clean, airy, and editorial aesthetic.

## Compliance Dashboard Recommendations Panel

The Service Minutes tab on `/compliance` renders `<RecommendationsPanel />` (`artifacts/trellis/src/components/compliance/RecommendationsPanel.tsx`) directly under the KPI cards. It surfaces deterministic, data-grounded operational guidance — **not AI**. Every recommendation is the output of a pure function (`buildRecommendations`) over real endpoint data; the runtime logic is the source of truth and `RECOMMENDATION_RULES` (exported from the same file) documents each rule for audit. Severity is one of `critical | high | medium`; the panel sorts by severity and renders an "All clear" empty state when no rule fires.

**Honesty guarantee:** the panel tracks every upstream data source it depends on (`/api/reports/compliance-risk-report`, `/api/reports/weekly-compliance-summary`, `/api/dashboard/summary`). If any of them fails or is missing, an inline "Recommendations may be incomplete" warning is rendered listing exactly which rules could not be evaluated. When no rules fire AND any data source is unavailable, the panel renders an amber "Recommendations couldn't be evaluated" state instead of the green "All clear" — so admins are never misled into thinking the district is healthy when the panel simply lacked data.

Rules currently implemented:

| ID | Severity | Trigger | Message | Action link | Source endpoint |
|---|---|---|---|---|---|
| `out-of-compliance-makeup` | critical | `summary.studentsOutOfCompliance > 0` | Schedule make-up sessions for N students who are out of compliance | `/compliance-risk-report` | `/api/reports/compliance-risk-report` |
| `at-risk-increase-coverage` | high | `summary.studentsAtRisk > 0` | N students at risk — increase service coverage this week | `/compliance-risk-report` | `/api/reports/compliance-risk-report` |
| `providers-under-75` | high | Any `providerSummary` row with `totalRequired > 0 && complianceRate < 75` | N providers below 75% delivery — review caseload (lowest provider named) | `/staff` | `/api/reports/compliance-risk-report` |
| `missed-sessions-this-week` | high if total missed ≥ 5, else medium | `weeklySummary.providersWithMissedThisWeek` has any provider with `missedSessions > 0` | X providers have incomplete logs — Y missed sessions this week | `/weekly-compliance-summary` | `/api/reports/weekly-compliance-summary` |
| `high-exposure-cases` | critical if any case ≥ $5,000, else high | `needsAttention` rows with `estimatedExposure ≥ $1,000` | Review N high-exposure cases totaling $X | `/compliance-risk-report` | `/api/reports/compliance-risk-report` |
| `outstanding-compensatory` | high | `summary.existingCompensatoryExposure > 0` | Outstanding compensatory obligations: $X — schedule make-up services | `/compensatory-services` | `/api/reports/compliance-risk-report` |
| `service-type-behind` | medium | A `complianceByService` row where `(atRisk + outOfCompliance) > onTrack` | {Service Type}: more students behind than on track (N flagged) — assess staffing | `/compliance-risk-report` | `/api/dashboard/compliance-by-service` |
| `uncovered-blocks-today` | high | `dashboardSummary.uncoveredBlocksToday > 0` | N scheduled session blocks have no provider assigned today | `/schedule` | `/api/dashboard/summary` |
| `large-attention-queue` | medium | `needsAttention.length ≥ 10` | Triage all N flagged service requirements in the full risk report | `/compliance-risk-report` | `/api/reports/compliance-risk-report` |

To add a new rule: add a branch in `buildRecommendations`, append a matching entry to `RECOMMENDATION_RULES`, and update this table. New rules must be deterministic (no LLM/heuristic calls) and grounded in an existing endpoint — do not market the panel as AI.

## District Onboarding Checklist

Trellis ships with a deterministic, completion-based onboarding checklist that walks a district from a blank workspace to a usable pilot. **No item is "checked off" by a button click — each one is derived from real database state**, so the panel always reflects what's actually been set up.

**Component:** `artifacts/trellis/src/components/onboarding/PilotOnboardingChecklist.tsx`. The exported constant `PILOT_CHECKLIST_ITEMS` is the source of truth for the eight steps and their action links.

**Data source:** `GET /api/onboarding/status` (in `artifacts/api-server/src/routes/onboarding.ts`). The endpoint returns a `pilotChecklist` object plus a `counts` object; the legacy `isComplete`/`completedCount`/`totalSteps` fields at the top level still drive the older 4-step `SetupChecklist` widget and are intentionally unchanged.

| # | Step (key) | Trigger (true when…) | Action link |
|---|---|---|---|
| 1 | District profile configured (`districtProfileConfigured`) | `district_confirmed` step recorded **AND** ≥1 school **AND** ≥1 service type | `/settings` |
| 2 | School year configured (`schoolYearConfigured`) | ≥1 row in `school_years` for the district, OR `district_confirmed` metadata contains a `schoolYear` | `/school-year` |
| 3 | Staff imported (`staffImported`) | ≥1 non-deleted row in `staff` | `/staff` |
| 4 | Students imported (`studentsImported`) | ≥1 non-deleted row in `students` | `/import` |
| 5 | Service requirements imported (`serviceRequirementsImported`) | ≥1 active row in `service_requirements` | `/students` |
| 6 | Providers assigned (`providersAssigned`) | ≥1 active row in `service_requirements` with `provider_id` not null | `/staff` |
| 7 | First sessions logged (`firstSessionsLogged`) | ≥1 non-deleted row in `session_logs` | `/sessions` |
| 8 | Compliance dashboard active (`complianceDashboardActive`) | Derived: students > 0 **AND** active requirements > 0 **AND** sessions > 0 | `/compliance` |

**Where the checklist appears:**
- **Pilot admin home (`/`)** — rendered as `<PilotOnboardingChecklist variant="compact" />` near the top of `PilotAdminHome.tsx`. Auto-collapses once `isComplete` is true and shows the next outstanding step inline.
- **Dedicated `/onboarding` page** — full-width `<PilotOnboardingChecklist variant="full" />` (always visible, even when complete), backed by `artifacts/trellis/src/pages/onboarding.tsx` and registered in `App.tsx`.

**Honesty guarantee:** if `/api/onboarding/status` fails, the compact variant renders nothing (silent on the dashboard) and the full variant renders an amber error card — it never falsely shows "Pilot ready" when the underlying data could not be loaded.

To add a new step: add a row to `pilotChecklist` in `onboarding.ts`, append a matching entry to `PILOT_CHECKLIST_ITEMS`, and update the table above.

## User Preferences

I want iterative development and detailed explanations of your thought process. Ask clarifying questions before making major architectural changes or implementing complex features. Do not change the fundamental project structure or core technologies without explicit approval.

## System Architecture

Trellis is structured as a monorepo using `pnpm` workspaces, clearly separating frontend and backend concerns.

**Technology Stack:**
-   **Frontend:** React 19, Vite, Tailwind CSS, shadcn/ui, Recharts, wouter.
-   **Backend (API):** Express 5, Node.js 24.
-   **Database:** PostgreSQL with Drizzle ORM.
-   **Data Validation:** Zod.
-   **API Generation:** Orval (generates React Query hooks and Zod schemas from OpenAPI 3.1).

**Core Architectural Decisions:**

-   **Modular Monorepo:** Code is organized into `artifacts/trellis` (frontend), `artifacts/api-server` (backend), `lib/api-spec` (OpenAPI spec), and shared libraries.
-   **RESTful API Design:** All backend interactions are exposed via a REST API.
-   **Authentication:** Clerk authentication provides real sign-in/sign-up flows and secures routes using `ProtectedRoutes`. Role-based access is enforced, with 9 distinct roles (`admin`, `case_manager`, `bcba`, `sped_teacher`, `coordinator`, `provider`, `para`, `sped_student`, `sped_parent`). Backend API authentication uses `@clerk/express` middleware to validate roles and tokens. In dev mode, `requireAuth` defaults to `admin` role and `requireDistrictScope` auto-resolves the first district from DB when Clerk metadata has no districtId.
-   **Guardian Portal:** The guardian portal sub-router is path-scoped at `/guardian-portal` in `routes/index.ts` — its `requireGuardianScope` middleware only applies to guardian routes, not to the rest of the API.
-   **Comprehensive Database Schema:** A PostgreSQL database manages districts, schools, students, staff, services, IEPs, compliance, ABA data, and protective measures, including detailed `restraint_incidents` for MA DESE 603 CMR 46.00.
-   **UI/UX Design:** Adheres to the "Ink & Air" theme, utilizing a pure white, neutral gray, and emerald green palette. Features role-based color theming and responsive design with components like `ProgressRing` and a role-aware `AppLayout`.

**Feature Specifications:**

-   **AI-Powered IEP PDF Import:** Upload IEP PDF documents (single or bulk up to 50) for automatic AI extraction of structured data using GPT-5.2. Extracts student name, IEP dates, PLAAFP sections, goals (with behavior/program target linkage), service requirements (with service type matching/creation), accommodations, behavior targets, and program targets. Student matching by external ID or first+last name (ilike). SSE streaming for bulk upload progress. All DB writes wrapped in transactions for atomicity. API: `POST /api/imports/iep-documents` (single), `POST /api/imports/iep-documents/bulk` (bulk with SSE). Route file: `artifacts/api-server/src/routes/imports/iepDocuments.ts`. Frontend: IEP Documents tab in import-data page with drag-drop multi-PDF upload, per-file progress tracking, and detailed results panel. Dependencies: `pdf-parse@1.1.1` (imported via `pdf-parse/lib/pdf-parse.js` to avoid test-file-on-import bug), `@workspace/integrations-openai-ai-server` (OpenAI SDK via Replit AI integrations).
-   **BCBA Clinical Tools:** Integrated FBA (Functional Behavior Assessment), Functional Analysis, and BIP (Behavior Intervention Plan) generation, including ABC data observation and function-specific strategies.
-   **IEP Workflow:** MA 603 CMR 28.00 compliant IEP pages, goal management (with auto-target creation), accommodations, meeting management, DESE-compliant progress reports with PDF generation, and parent notifications. IEP Builder auto-save: wizard state is server-persisted (`iep_builder_drafts` table) on step transitions and via 30s debounce. On re-entry, a "Resume or Start Fresh" dialog restores the exact step and field values. A "Draft saved [time]" indicator appears in the wizard header. Drafts are cleaned up both client-side and server-side upon IEP generation. API: `GET/PUT/DELETE /api/students/:id/iep-builder/draft`.
-   **IEP Program Suggestions:** An engine generates suggestions for behaviors, DTTs, task analyses, academic programs, and related services based on IEP goals, with relevance scoring and one-click application.
-   **Protective Measures (603 CMR 46.00/46.06):** Full MA DESE-compliant tracking for restraint/seclusion/time-out incidents, featuring a 5-step full form, compliance checklist, digital signatures, DESE CSV/JSON export, a Quick Report 2-step form for fast incident capture (creates draft), auto-save draft indicator (localStorage-backed with 30s debounce and step-transition triggers), and an always-expanded Incident Trends panel (monthly volume chart, top antecedents, high-frequency students).
-   **Session-IEP Goal Integration:** Sessions can be linked to IEP goals with inline data collection for behavior and program data.
-   **IEP Goal Progress Visualization (Task #152):** Per-goal trend charts on the student detail page showing progress data points over time with baseline and mastery criterion reference lines, trend direction indicators (improving/stable/declining), progress ratings (mastered/on track/some progress/needs attention), and interactive charts with date and staff filtering. Clickable data points show session details including trial counts (correct/total), session type, and session notes in an expandable detail panel. Chart export as PNG via html-to-image for inclusion in IEP meeting materials (download button in chart toolbar). API: `GET /api/students/:id/iep-goals/progress` returns all active goals with linked program or behavior target time-series data; each data point includes `dataSessionId`, `notes`, `trialsCorrect`, `trialsTotal`, and `sessionType`.
-   **Student Snapshot (Task #151):** At-a-glance view as the first tab on the student detail page. Aggregated single-endpoint API (`GET /api/students/:id/snapshot`) returns: active goals with progress ratings and trend directions, upcoming deadlines (IEP annual review, re-evaluation, team meetings) with urgency indicators, last 5 sessions, recent restraint incidents, active accommodations, compliance status (service minute tracking), and active alerts. Card-based UI: 4 summary stat cards (goals, compliance %, sessions, alerts), then detailed Goal Progress card (scrollable list with trend arrows and rating badges), Upcoming Deadlines card (color-coded urgency), Recent Sessions card, Safety & Incidents card, Active Accommodations grid, and Active Alerts panel. Performance-optimized with batched program/behavior data queries (2 total vs N+1). Tenant-scoped via `assertStudentAccess`. Component: `artifacts/trellis/src/components/student-snapshot.tsx`.
-   **IEP Meeting Prep Checklist (Task #153):** Guided readiness tracking for IEP team meetings. DB table: `meeting_prep_items` (meetingId FK cascade, itemType, label, description, required, autoDetected, manuallyUnchecked, completedAt, completedByStaffId FK, notes, sortOrder; unique constraint on meetingId+itemType). 8 default checklist items auto-created on first access: gather_progress_data, draft_review_goals, contact_parent, confirm_attendance, prepare_pwn, set_location, review_accommodations, prepare_agenda. Auto-detection scans existing data (sessions in last 90d, active goals, parent messages in last 30d, attendees, PWNs, meeting location, accommodations, agenda items) to auto-complete items; `manuallyUnchecked` flag prevents re-auto-checking items users intentionally unchecked. Readiness percentage (total and required-only) displayed as progress bar on prep panel and as inline indicator on meetings list. Draft agenda generator builds structured agenda sections from meeting type, student goals, accommodations, and attendees. API: `GET /api/iep-meetings/:id/prep` (auto-creates items + auto-detects + returns readiness), `PATCH /api/iep-meetings/:id/prep/:itemId` (toggle completion with notes, 2000 char limit), `GET /api/iep-meetings/:id/agenda` (structured sections with attendees and goals). Frontend: `MeetingPrepChecklist` component with required/optional grouping, auto-detected badge, per-item notes, collapsible draft agenda viewer. `ReadinessIndicator` component shows color-coded progress bar (green ≥80%, amber ≥50%, red <50%) on meetings list. "Prep" tab added to MeetingDetail panel. Component: `artifacts/trellis/src/components/meeting-prep-checklist.tsx`.
-   **Team Collaboration Notes (Task #154):** Shared student-level comment threads for team coordination. DB tables: `student_notes` (studentId FK, authorStaffId FK, content 5000 char limit, pinned, mentions JSONB array of staff IDs, parentNoteId for threading, soft delete), `student_note_mentions` (noteId FK cascade, mentionedStaffId FK, notified flag). Migrations: `013_student_notes.sql`. @mention syntax: `@[staffId]` parsed from content, validated against staff table, creates `note_mention` alerts for mentioned staff. Notes panel on student detail page with compose box, @mention autocomplete dropdown (filters active staff by name/role), pinned notes sorted first, role-colored author badges, time-ago timestamps, pin/unpin toggle, delete (author or admin only), edit content (author or admin only). Filtering: by author (dropdown), pinned-only toggle, clearable. Empty state with icon. Collapsible card. API: `GET /api/students/:id/notes` (filterable by author, pinned, date range; joins staff for authorName/authorRole), `POST /api/students/:id/notes` (extracts @mentions, validates staff IDs, creates mention records + alerts, requires tenantStaffId), `PATCH /api/students/:id/notes/:noteId` (edit content or toggle pin), `DELETE /api/students/:id/notes/:noteId` (soft delete, author/admin only), `GET /api/students/:id/notes/staff` (active staff list for mention autocomplete). Route file: `artifacts/api-server/src/routes/studentNotes.ts`. District-scoped via `assertStudentAccess`, staff-role-gated. Audit-logged create/update/delete. Component: `artifacts/trellis/src/components/student-notes.tsx`.
-   **Accommodation Tracking & Verification (Task #155):** Gen ed teacher verification workflow for IEP accommodations. DB table: `accommodation_verifications` (accommodationId FK cascade to iep_accommodations, verifiedByStaffId FK, status enum verified/partial/not_implemented/not_applicable, notes 2000 char, periodStart/End). Migration: `014_accommodation_verifications.sql`. 30-day verification window defines "overdue" status. API: `GET /api/students/:id/accommodation-summary` (active accommodations grouped by category with last verification, overdue flags, verification rate), `POST /api/accommodations/:id/verify` (staff records verification with status/notes), `GET /api/accommodations/:id/verifications` (history), `GET /api/accommodation-compliance` (district-wide compliance dashboard — total students, overall rate, per-student breakdown), `GET /api/students/:id/accommodation-card` (printable accommodation summary). Auth: `/students/*` routes inherit existing staff guards; `/accommodations/*` requires district scope + staff role; `/accommodation-compliance` requires district scope + privileged staff. Frontend: `AccommodationTracking` component on student detail page (collapsible card with verification stats, per-accommodation verify/history inline, print card button), `AccommodationLookup` page at `/accommodation-lookup` (district compliance dashboard with search, overdue/compliant student grouping, click-through to student detail). Sidebar: "Accommodations" link under Compliance section. Component files: `artifacts/trellis/src/components/accommodation-tracking.tsx`, `artifacts/trellis/src/pages/accommodation-lookup.tsx`. Route file: `artifacts/api-server/src/routes/accommodationVerifications.ts`.
-   **Medicaid Billing Integration (Task #156):** CPT code mapping, claim generation, review queue, and export for Medicaid reimbursement. DB tables: `cpt_code_mappings` (service_type_id → CPT code + modifier + rate per unit + duration tiers), `medicaid_claims` (one claim per session, status lifecycle: pending→approved→exported or rejected/void, with CPT code, units, billed amount, district scoping). Staff schema extended with `npiNumber` and `medicaidProviderId`; students extended with `medicaidId`. API routes under `/api/medicaid/*`: CPT mapping CRUD, claim generation engine (matches completed sessions to CPT codes with duration-based tier selection, skips duplicates), claims list with filters (status, date range, service type, provider), batch approve/reject/void, 837P-style CSV and JSON export with batch IDs, revenue summary (by service type and by month with data quality metrics). All endpoints admin/coordinator role-gated and district-scoped. Frontend: `/medicaid-billing` page with 4 tabs — Claims Queue (status filters, batch select, approve/reject), CPT Mappings (editor with common CPT code presets), Revenue Dashboard (KPI cards + charts), Export (CSV/JSON download). Staff detail page shows editable "Medicaid Billing Info" card (NPI Number + Medicaid Provider ID) for admin/coordinator. Student detail page shows editable Medicaid ID field for admin/coordinator. Route file: `artifacts/api-server/src/routes/medicaidBilling.ts`. Schema files: `lib/db/src/schema/medicaidClaims.ts`, `lib/db/src/schema/cptCodeMappings.ts`.
-   **Compliance Risk Forecasting / Cost Avoidance Dashboard (Task #157):** Predictive compliance risk engine with financial exposure estimates at `/cost-avoidance`. Three risk categories: (1) Evaluation deadlines — open/in-progress evaluation referrals and compliance events approaching or past deadline, estimated comp cost based on days overdue; (2) Service shortfalls — active service requirements trending below delivery targets (monthly: projected shortfall based on pace at month-progress; weekly: end-of-week gap detection), exposure calculated from service type billing rates × shortfall hours; (3) IEP annual reviews — active IEPs with end dates within 90 days and no annual review meeting scheduled, escalating exposure by proximity. Risk urgency levels: critical (0-7 days / overdue), high (8-14 days), medium (15-30 days), watch (31-90 days). Dashboard UI: total exposure banner with urgency breakdown cards, exposure-by-category horizontal bar chart, urgency distribution pie chart, category filter pills, collapsible urgency-grouped risk list with per-item student drill-down links, action recommendations, and dollar exposure. "Send Risk Alerts" button generates `cost_avoidance_risk` alerts for critical/high items (deduped by message). API: `GET /api/cost-avoidance/risks` (full risk items + summary), `GET /api/cost-avoidance/summary` (aggregate stats only), `POST /api/cost-avoidance/generate-alerts` (creates alerts for critical/high risks). Admin/coordinator role-gated, district-scoped. Route file: `artifacts/api-server/src/routes/costAvoidance.ts`. Frontend: `artifacts/trellis/src/pages/cost-avoidance.tsx`. Sidebar: "Cost Avoidance" under District section.
-   **Compensatory Services Financial Tracker (Task #158):** Dollar-value exposure view for compensatory service obligations at `/compensatory-finance`. DB table: `service_rate_configs` (districtId, serviceTypeId, inHouseRate, contractedRate, effectiveDate, notes; unique constraint on district+serviceType+effectiveDate). Rate resolution cascade: district rate config → agency contract rate → service type default rate → $75/hr fallback. Financial calculation engine computes dollar values from `compensatory_obligations.minutesOwed/minutesDelivered` × applicable hourly rate. Dashboard UI: total outstanding obligation banner with gradient, KPI cards (total owed, delivered, students affected, fulfillment rate), 3 tabs — Overview (by-service-type and by-school bar charts, burn-down line chart showing accrued vs delivered vs cumulative outstanding over 12 months, by-provider list), Student Balances (sortable table with expandable per-student service breakdown, progress bars, drill-down links), Rate Configuration (CRUD for in-house/contracted rates per service type with effective dates). CSV export with formula-injection protection for budget planning. API: `GET /api/compensatory-finance/overview` (district roll-up by service type, school, provider), `GET /api/compensatory-finance/students` (per-student balances with service breakdown), `GET /api/compensatory-finance/burndown` (monthly accrual vs delivery trend), `GET /api/compensatory-finance/export.csv` (downloadable report), `GET/POST/DELETE /api/compensatory-finance/rates` (rate config CRUD). Admin/coordinator role-gated, district-scoped. Route file: `artifacts/api-server/src/routes/compensatoryFinance.ts`. Schema: `lib/db/src/schema/serviceRateConfigs.ts`. Frontend: `artifacts/trellis/src/pages/compensatory-finance.tsx`. Sidebar: "Compensatory Finance" under District section.
-   **Resource Management:** Provider utilization and budget tracking for staff and services.
-   **Caseload Balancing (Task #147):** Admin tool at `/caseload-balancing` to visualize provider workload distribution and rebalance caseloads. Horizontal bar chart (Recharts) showing student counts per provider with color-coded status (green=balanced, amber=approaching, red=overloaded). Configurable per-role thresholds (default: BCBA 15, case_manager 25, provider 30, etc.) via threshold dialog. Summary cards: total providers, overloaded, approaching, balanced counts. Role-level summary with averages. Provider detail panel showing assigned students with reassignment capability. Automated rebalancing suggestions prioritizing same-school transfers. Reassignment updates both `staff_assignments` and `service_requirements` tables with audit logging. Print-friendly layout. API: `GET /api/caseload-balancing/summary` (provider caseloads + role summary + totals), `GET /api/caseload-balancing/provider/:id/students`, `GET /api/caseload-balancing/suggestions`, `POST /api/caseload-balancing/reassign`. Route file: `artifacts/api-server/src/routes/caseloadBalancing.ts`. District-scoped, role-gated via `requirePrivilegedStaffOnly`.
-   **Staff Scheduling & Availability (Task #144):** Cross-building provider calendar at `/staff-calendar`. DB table: `staff_schedules` (staffId, schoolId, serviceTypeId, dayOfWeek, startTime, endTime, label, notes, effectiveFrom, effectiveTo). Migrations: `008_staff_schedules.sql`, `009_staff_schedules_service_type.sql`. Weekly grid view showing all providers color-coded by school assignment with filters (staff, school, service type). Conflict detection: overlapping time blocks for the same provider across buildings highlighted with red ring, date-range aware (effectiveFrom/To overlap check), with automated resolution suggestions (shorten block, move start time, consolidate). Coverage gap detection: time-slot granularity (hourly windows within 7AM–4PM school hours), showing specific uncovered time ranges per school/day. Provider availability panel: shows when filtering by provider — weekly hours, days scheduled, per-school distribution bar chart, free time slots per day. Provider profile integration: staff-detail page shows "Weekly Schedule & Availability" card with building distribution, day-by-day blocks, and free slots. Summary stats: providers count, total blocks, weekly hours. Admin/coordinator CRUD with server-side conflict validation (409 on overlap, date-range aware). Service type tracking per schedule block. Audit-logged create/update/delete. API: `GET /api/staff-schedules` (filterable by staffId, schoolId, dayOfWeek, serviceTypeId), `GET /api/staff-schedules/conflicts` (with suggestions array), `GET /api/staff-schedules/coverage-gaps` (time-slot granularity with uncoveredSlots), `GET /api/staff-schedules/provider-summary/:staffId` (with availability map), `POST /api/staff-schedules`, `PUT /api/staff-schedules/:id`, `DELETE /api/staff-schedules/:id`. Route file: `artifacts/api-server/src/routes/staffSchedules.ts`. District-scoped and staff-role-gated.
-   **Substitute & Coverage Management (Task #148):** Admin tool at `/coverage` for managing provider absences and substitute assignments. DB tables: `staff_absences` (staffId, schoolId, absenceDate, absenceType, startTime/endTime, notes, reportedBy), `coverage_instances` (scheduleBlockId, absenceDate, originalStaffId, substituteStaffId, isCovered, absenceId, notes), `schedule_blocks` (recurring schedule templates). Features: 5-tab interface (Uncovered Sessions, Staff Absences, Coverage Log, Usage Report, Workload). Daily summary cards showing total/covered/uncovered sessions and coverage rate. "Mark Absent" logs absence and auto-flags affected schedule blocks as uncovered. One-click substitute assignment with in-app notification to covering provider (creates alert). Coverage log tab shows historical covered sessions with original/substitute provider info. Usage report tab shows substitute usage patterns with bar chart and per-provider stats (sessions covered, unique dates, providers covered for). Absences view shows per-provider absence patterns with coverage rate %. Workload tab shows staff scheduled hours with overload detection. API: `GET /api/coverage/summary` (daily stats), `GET /api/coverage/history` (covered sessions log), `GET /api/coverage/substitute-report` (substitute usage patterns + absence frequency), `GET /api/schedule-blocks/uncovered` (uncovered sessions), `POST /api/schedule-blocks/:id/assign-substitute` (assigns sub + creates alert), `POST /api/staff/:id/absences` (marks absent + creates coverage instances), `GET /api/staff/:id/absences`, `DELETE /api/absences/:id`. Route file: `artifacts/api-server/src/routes/schedules.ts`. Admin/coordinator role-gated, district-scoped.
-   **Compensatory Services:** Tracks owed compensatory minutes, calculates shortfalls, and manages compensation sessions.
-   **Parent Communication:** Manages parent contacts, follow-ups, and compliance notifications. Includes a "Share Progress" feature for secure progress report sharing. **Parent Communication Hub (Task #143):** In-app messaging system between staff and guardians with threaded conversations. DB tables: `parent_messages` (sender/recipient staff/guardian, studentId, threadId, category, subject, body, readAt, metadata), `message_templates` (name, category, subject, body with {{placeholders}}, isSystem, districtId for tenant scoping), `conference_requests` (staffId, guardianId, proposedTimes JSONB, selectedTime, status proposed/accepted/declined/cancelled). Migration: `007_parent_messages.sql`. 5 seeded system templates: Prior Written Notice, IEP Meeting Invitation, Progress Update, General Message, Conference Request. Template placeholders auto-filled: `{{studentName}}`, `{{guardianName}}`, `{{staffName}}`, `{{subject}}`, `{{body}}`. Staff-side: Messages section on student detail page with compose dialog, template picker, threaded view with read receipts, search bar (ILIKE on subject/body with category/date filters), conference scheduling with multiple proposed times, auto mark-read when expanding threads with unread guardian replies. Guardian-side: Messages inbox page at `/guardian-portal/messages` with unread counts, reply capability, conference accept/decline flow. Home page updated with Messages card showing unread count. Calendar integration: accepted conferences auto-create `team_meetings` records with meeting type `parent_conference`. Email notifications: fire-and-forget `sendEmail()` on staff→guardian messages and conference requests (via Resend when configured, audit-logged when not). All create/update actions audit-logged (staff sends, guardian replies, conference decisions). Security: district ownership validation on all staff endpoints, guardian-student relationship checks, category enum validation, district-scoped template retrieval (system + own district). API routes in `parentMessages.ts`: `GET/POST /students/:studentId/messages`, `GET /students/:studentId/messages/search`, `PATCH /students/:studentId/messages/:messageId/read`, `GET/POST /students/:studentId/conference-requests`, `GET /message-templates`. Guardian routes: `GET /guardian-portal/messages`, `PATCH /guardian-portal/messages/:id/read`, `POST /guardian-portal/messages/:id/reply`, `GET /guardian-portal/conferences`, `PATCH /guardian-portal/conferences/:id`.
-   **ABA Clinical Graphing & IOA Tracking:** Per-target line graphs with phase change lines (vertical dashed), least-squares trend lines, aim lines (baseline→goal), and PNG/SVG export. Inter-observer agreement (IOA) tracking with paired observations, automatic agreement calculation (point-by-point for frequency/interval, exact for duration), and per-target IOA summaries with 80% threshold indicators.
-   **Para Mobile Data Entry ("My Day"):** Mobile-first daily agenda for paraprofessionals showing schedule blocks, quick-start session timer with auto-populated fields, guided data collection with step-by-step DTT trial recording (correct/incorrect + prompt level), behavior frequency tally counters, and read-only IEP goal summaries. All controls use 44px+ touch targets. API: `GET /para/my-day`, `GET /para/student-targets/:studentId`. Para role has dedicated simplified navigation.
-   **Quick Log (Mobile Session Wizard):** Full-screen step-by-step session entry component (`QuickLogSheet`) accessible via a floating FAB on My Day and per-block "Log Session"/"Missed" buttons. Steps: Student picker (with localStorage recents) → Service picker → Duration selector (preset 15/20/30/45/60 min + custom) → Completed/Missed toggle → [Missed path: reason quick-picks + make-up flag] → Note → Submit. "Mark Missed" blocks pre-set outcome and jump directly to reason step. Defaults (last duration, recent students/services) persisted in `trellis_quicklog_v1` localStorage key. Past unlogged schedule blocks surface an amber alert banner at top of My Day. Submits to `POST /api/sessions`.
-   **Live Session Data Collection:** Integrated data collection during timed sessions via `LiveDataPanel` component (`artifacts/trellis/src/components/live-data-panel/`). FloatingTimer cards include a "Collect" button that opens the data panel as an overlay or popup window. Features: IEP goal picker with category filters (Academic, Behavior, etc.) and Select All; per-goal data widgets — `BehaviorWidget` (frequency counter, duration sub-timer, interval recording per measurementType), `ProgramWidget` (Correct/Incorrect trial buttons with running %, prompt level chips IND/V/G/M/PP/FP, trial history undo stack), `GoalNotesWidget` (plain notes); running timer display; data summary badges. Popup window support via `BroadcastChannel` for cross-window state sync with fallback inline overlay. Collected data persisted in `TimerEntry.collectedData` (Record<string, CollectedGoalEntry>) in localStorage (`trellis_session_timers_v3_${userId}`). On timer stop, goal data flows through `QuickLogSheet` to `POST /api/sessions` as `goalData` array. ReviewStep shows linked goal count. Route: `/data-panel` for standalone popup page. Component files: `LiveDataPanel.tsx`, `BehaviorWidget.tsx`, `ProgramWidget.tsx`, `GoalNotesWidget.tsx`, `types.ts`, `useDataPanelPopup.ts`.
-   **Document Management & E-Signatures:** Object storage-backed document management per student. Staff can upload, categorize (IEP, evaluation, consent, progress report, etc.), download, and soft-delete documents. E-signature workflow: staff create signature requests with recipient name/email, generating a unique token-based signing link. Public `/sign/:token` page shows document info, canvas signature pad, and submit button. Signature data includes timestamp and IP for audit. Tokens are not leaked in list responses; signing enforces data-URI format, 500KB size limit, and 30-day expiry. Storage routes require auth. DB tables: `documents`, `signature_requests`. API: `/api/documents/*`, `/api/signature-requests/:token` (public). Frontend: `StudentDocuments` component on student detail page, `SignDocumentPage` at `/sign/:token`.
-   **FERPA Audit Logging:** Append-only audit trail tracking all access and modifications to student records. Logs actor identity (Clerk userId + role), action type (create/read/update/delete), target table and record, affected student, IP address, summary text, and old/new value diffs for mutations. Admin-only viewer at `/audit-log` with filters (action, table, date range, search), stats summary, detail dialog, and CSV export. Instrumented routes: students (CRUD + detail read), IEP goals (CRUD), sessions (create), protective measures (create). Backend: `audit_logs` table with indexes on actor, action, target, student, timestamp. Helper: `logAudit()` fires-and-forgets inserts to avoid blocking request latency.
-   **Admin Onboarding Wizard:** SIS-first setup flow at `/setup` accessible from admin sidebar. Four guided steps: (1) SIS Connection — select provider (PowerSchool, Infinite Campus, Skyward, CSV) and enter district/school info; (2) District & Schools — confirm/edit pulled data; (3) Service Types — select from 12 common SPED service types; (4) Staff Invite — add team members by name/email/role. Persistent `SetupChecklist` widget shows on the admin dashboard when setup is incomplete (auto-hides when all steps are done). API: `GET /api/onboarding/status`, `POST /api/onboarding/sis-connect`, `POST /api/onboarding/district-confirm`, `POST /api/onboarding/service-types`, `POST /api/onboarding/invite-staff`. All mutation endpoints admin-only, status endpoint admin/coordinator, with idempotent upsert logic and duplicate prevention.
-   **Settings Hub:** Consolidated admin settings at `/settings` with 7 tabs: General (setup wizard), School Year, SIS Integration, Audit Log, Recently Deleted, System Status, Legal & Compliance. Old direct URLs (`/audit-log`, `/school-year`, `/sis-settings`, `/recently-deleted`, `/system-status`, `/legal-compliance`, `/setup`) redirect to `/settings#<tab>`. Admin sidebar reduced to 5 items: Reports, Analytics, Data Import, Billing, Settings.
-   **Soft Delete & Recovery:** Students, staff, session logs, and schedule blocks use soft-delete (`deletedAt` timestamp) instead of permanent removal. All list queries filter out soft-deleted records. Admin "Recently Deleted" page (now under Settings → Recently Deleted tab) shows deleted records across all 4 entity types with one-click restore.
-   **Emergency Contacts & Medical Alerts (Task #57):** Safety data per student. DB: `emergency_contacts` table (student_id FK, firstName, lastName, relationship, phone, phoneSecondary nullable, email nullable, isAuthorizedForPickup boolean default false, priority integer default 1, notes); `medical_alerts` table (student_id FK, alertType text enum allergy/medication/condition/seizure/other, description, severity text enum mild/moderate/severe/life_threatening, treatmentNotes nullable, epiPenOnFile boolean default false, notifyAllStaff boolean default false). Schema exports `MEDICAL_ALERT_TYPES` and `MEDICAL_ALERT_SEVERITIES` const arrays. API: Emergency contacts — `GET /api/students/:id/emergency-contacts`, `POST /api/students/:id/emergency-contacts`, `PATCH /api/emergency-contacts/:id`, `DELETE /api/emergency-contacts/:id`. Medical alerts — `GET /api/students/:id/medical-alerts`, `POST /api/students/:id/medical-alerts`, `PATCH /api/medical-alerts/:id`, `DELETE /api/medical-alerts/:id`. Read roles: admin/case_manager/sped_teacher/para/provider/coordinator/bcba. Write roles: admin/case_manager only. Strict enum validation on alertType+severity in POST/PATCH. All mutations audit-logged. Frontend: Two new cards in student-detail.tsx (after StudentGuardians, before Enrollment History): "Emergency Contacts" (Phone icon, prioritized list, primary badge on first, Authorized Pickup badge, clickable phone/email links, edit/delete for admin+case_manager) and "Medical Alerts" (Stethoscope icon, color-coded severity badges mild=blue/moderate=amber/severe=orange/life_threatening=red, EpiPen indicator, Notify All Staff badge, Life-Threatening header warning badge). Quick-view card: `StudentQuickView` component (`components/student-quick-view.tsx`) attached to phone icon button beside each student name on students list. Click loads emergency contacts + severe/life-threatening alerts lazily; shows primary contact name, phone, authorized-pickup status, and life-threatening/severe alert list. Panel closes on outside click or X button.
-   **Enrollment History (Task #56):** Tracks student enrollment lifecycle events. DB: `enrollment_events` table (student_id FK, eventType text, eventDate text, reasonCode text, reason, notes, performedById FK, recordedById FK, fromSchoolId FK, toSchoolId FK, fromProgramId FK, toProgramId FK); students table extended with `enrolledAt`/`withdrawnAt` text fields. Schema exports `ENROLLMENT_EVENT_TYPES` (10 canonical values: enrolled, reactivated, withdrawn, transferred_in, transferred_out, program_change, graduated, suspended, leave_of_absence, note) and `ENROLLMENT_REASON_CODES` (graduation, transfer, family_move, program_completion, other). API: `GET /api/students/:id/enrollment` (read-only; admin/case_manager/sped_teacher/coordinator/bcba), `POST /api/students/:id/enrollment` (log event; admin/case_manager only; strict enum validation for eventType + reasonCode; transactional 4-state lifecycle: active/inactive/transferred/graduated updated atomically), `PATCH /api/students/:id/enrollment/:eventId` (admin/case_manager only; reads current event, merges effective type+date, reconciles student lifecycle fields in same transaction), `POST /api/students/:id/archive` (admin-only: transactional status=inactive + withdrawal event), `POST /api/students/:id/reactivate` (admin-only: transactional status=active + reactivated event). All actions audit-logged. Student list API defaults to status=active; status=all skips the predicate. CSV exports (active-ieps.csv, service-minutes.csv) accept ?status=active|inactive|all with active default. Frontend: Students page has Active/Inactive/All Statuses toggle (passes explicit status= param); inactive badge on student cards. Student detail: Archive/Reactivate header buttons (admin-only). Enrollment History timeline at bottom with 10-type color-coded events; Add Event dialog with 10-type dropdown and 5-value reasonCode Select.
-   **Error Boundaries:** Route-level React Error Boundaries wrap every page component via `BoundedRoute` in `App.tsx`. A crash in one page displays a friendly error message with retry button without affecting the rest of the application. Errors are reported to Sentry if `VITE_SENTRY_DSN` is set.
-   **Operational Monitoring:** Health check endpoints at `/health` (root, for uptime monitors) and `/api/health` (for internal use) return `{ status, db, uptime, version, timestamp }` with a 503 if the database is unreachable. Structured error handler in `app.ts` captures 5xx errors with context (method, URL, status) and optionally reports to Sentry via a lightweight envelope API sender in `artifacts/api-server/src/lib/sentry.ts`. Frontend Sentry initialized via `@sentry/react` in `main.tsx`. Admin "System Status" page at `/system-status` shows live API/DB health, uptime, and Sentry connection status. Server-side Sentry uses native `fetch` to send to Sentry's envelope API (no `@sentry/node` SDK) to avoid OpenTelemetry build conflicts; activate by setting `SENTRY_DSN` (server) and `VITE_SENTRY_DSN` (frontend) environment secrets.
-   **API Rate Limiting:** Two-tier rate limiting — 200 req/min general limit + 60 req/min mutation limit (POST/PUT/PATCH/DELETE) per IP.
-   **Theme System:** 10 built-in themes controlled via `ThemeProvider` in `lib/theme-context.tsx`. Default: "Open Air" (borderless, minimal — no card borders/shadows, thin divider lines, emerald left-bar active indicator). Other themes: Classic, Warm, Cool, High Contrast, Large Text, Extra Large Text, Deuteranopia-Safe, Protanopia-Safe, Reduced Motion. Theme stored in localStorage (`trellis-theme`). CSS variable overrides in `index.css` under `:root.theme-*` selectors. ThemePicker component in sidebar footer (palette icon). Card component uses `border-card-border` so cards become borderless automatically in Open Air.
-   **Clinical Supervision Tracking:** Tracks BCBA supervision of RBTs and paraprofessionals, including session logging and compliance dashboards.
-   **Evaluation & Eligibility Tracking (IDEA/603 CMR 28.04):** Full evaluation lifecycle management — referral intake (source, concerns, consent tracking), evaluation assignments (areas, lead evaluator, due dates), and eligibility determinations (disability categories, re-evaluation cycles). Dashboard with compliance metrics: open referrals, pending consent, overdue evaluations, upcoming re-evaluations. 30-school-day deadline calculation (≈45 calendar days). Role-restricted to admin, coordinator, case_manager, sped_teacher, bcba. DB tables: `evaluation_referrals`, `evaluations`, `eligibility_determinations`. API: `/api/evaluations/*`. Frontend: `/evaluations` page with List view (4 tabs: Dashboard, Referrals, Evaluations, Eligibility) and Pipeline/Kanban view (4 columns: Referral → In Progress → Completed → Eligibility Determined). Pipeline is default on desktop, List on mobile. Toggle via List/Pipeline buttons in page header. Pipeline cards show student name, grade, deadline badges, consent status, and eligibility outcome.
-   **Transition Planning (IDEA §300.320(b)):** Post-secondary transition planning for students aged 14+. Domain-specific goal entry across education, employment, and independent living. Agency referral tracker (VR, adult services, etc.) with follow-up date tracking and overdue alerts. Graduation pathway and credit documentation. Dashboard surfacing: students approaching transition age (13), students 14+ missing plans, overdue agency follow-ups. Student detail page shows transition section conditionally for age-eligible students. DB tables: `transition_plans`, `transition_goals`, `transition_agency_referrals`. API: `/api/transitions/*` (list endpoint returns `goalsCount`, `referralsCount`, `assessmentsUsed`, `studentVisionStatement` for progress computation). Frontend: `/transitions` page with Dashboard, Plans (with progress badges), and Plan Detail tabs. Progress badges on plan cards show completion % based on 5 criteria: graduation pathway, vision statement, assessments used, goals added, agency referrals added. Color-coded bar (red < 60%, amber 60–99%, green 100%). Badge refreshes after goal/referral mutations. Nav links added for admin and SPED teacher roles.
-   **IEP Meeting Management:** Full meeting lifecycle — scheduling (9 meeting types), normalized attendees (15 roles per 603 CMR 28.00), prior written notices (PWN/N1/N2), and parental consent tracking. Enhanced `team_meetings` table with IEP document and school linkage. Normalized sub-tables: `iep_meeting_attendees`, `prior_written_notices`, `meeting_consent_records`. Dashboard with overdue/upcoming/this-week counts, pending consent alerts, and annual review gap detection. IEP calendar integration surfaces scheduled meetings as `team_meeting` events. Main dashboard shows meeting alert card. API: `/api/iep-meetings/*` (CRUD + attendees, notices, consent, complete action, dashboard). Frontend: `/iep-meetings` page with Overview and All Meetings tabs, meeting detail panel with Info/Team/PWN/Consent sub-tabs. Nav links in admin (Students & Compliance) and SPED teacher (IEP & Clinical) sections.
-   **State Reporting & Data Export:** Admin-only state reporting page at `/state-reporting` for generating mandated data exports. Two report templates: IDEA Part B Child Count (federal) and MA SIMS Student Export (Massachusetts). Configurable export engine maps internal data fields to state-required column formats. Pre-export validation checks for missing required fields (SASID, DOB, disability category, active IEP) and surfaces errors/warnings before download. CSV exports with proper headers and escaping. Export history log tracks all generated reports (type, parameters, user, timestamp, record count, warning count). School-scope filtering for per-building or district-wide exports. DB table: `export_history`. API: `/api/state-reports/templates`, `/api/state-reports/validate`, `/api/state-reports/export`, `/api/state-reports/history`. Frontend: `/state-reporting` page with report type selector cards, school filter, validate button, export button, and export history table. Architecture is extensible for additional state formats.
-   **Automated Progress Reports (Task #150):** One-click IEP progress report generation at `/progress-reports`. Auto-generates narrative text describing progress toward each IEP goal with data citations from sessions, program data, and behavior data. Trend summary per goal (improving/maintaining/declining) with MA progress codes (M/SP/IP/NP/R/NA) per 603 CMR 28.07(8). Service delivery breakdown with compliance percentages. Draft → review → final → sent workflow with server-side status transition validation (strict FSM — rejects invalid transitions). PDF export via print-to-PDF with professional layout including district branding, student info, goal sections, data tables, and compliance bars. XSS-safe HTML generation with `escapeHtml()` for all dynamic content. Batch generation for entire caseload (up to 100 students) with partial-success reporting. Editable narratives, recommendations, and parent notes before finalizing. DB table: `progress_reports` (JSONB goal_progress, service_breakdown). API: `GET /api/progress-reports/all` (list all), `GET /api/progress-reports/:id` (detail), `POST /api/students/:id/progress-reports/generate` (single), `POST /api/progress-reports/batch-generate` (batch), `PATCH /api/progress-reports/:id` (edit + status transitions). Frontend: `artifacts/trellis/src/pages/progress-reports.tsx`. Nav: under Compliance section.
-   **Committee Reporting & Analytics Export (Task #145):** Pre-configured committee report types with both CSV and PDF output. Four report types: Compliance Summary (per-student compliance status with service delivery breakdown), Services by Provider (session counts, minutes, students by provider), Student Roster (full SPED roster with disability/IEP status), Caseload Distribution (staff caseload sizes by role). Export History tracking (`export_history` table with districtId, format columns) and Scheduled Reports (`scheduled_reports` table with frequency weekly/monthly, recipient emails, next run calculation). Frontend Exports tab has 3 sections: Generate Reports (committee PDF/CSV + data CSV), Export History (last 50 runs), Scheduled Reports (CRUD). Scheduler integrated into reminders.ts — `runScheduledReports()` checks due schedules every 6 hours, generates CSV via direct `generateReportCSVDirect()` function (no HTTP roundtrip), records to export_history, and advances nextRunAt. All endpoints district-scoped via `resolveExportScope()`. reportType validated against whitelist. DB tables: `export_history` (extended), `scheduled_reports`. Migration: `010_scheduled_reports.sql`. API: `GET /api/reports/exports/{type}.csv`, `GET /api/reports/exports/{type}.pdf`, `GET /api/reports/exports/history`, `GET|POST /api/reports/exports/scheduled`, `DELETE /api/reports/exports/scheduled/:id`. Route file: `artifacts/api-server/src/routes/reportExports.ts`.
-   **Document Approval Workflow (Task #146):** Version history and configurable approval chain for IEP documents. DB tables: `document_versions` (document_type, document_id, student_id, district_id, version_number, title, change_description, snapshot_data, author), `approval_workflows` (document_type, document_id, student_id, district_id, title, current_stage, stages JSONB, status in_progress/completed/rejected, created_by), `workflow_approvals` (workflow_id FK, stage, action approved/rejected/changes_requested, reviewer, comment). Default 4-stage pipeline: draft → team_review → director_signoff → parent_delivery. Configurable stages per workflow. Three reviewer actions: approve (advance to next stage), reject (terminate workflow), request changes (reset to draft). PWN auto-generation from IEP meeting data (pre-populates goals, team decisions, required 603 CMR 28.07(1) fields). Dashboard with summary cards (active/completed/rejected counts, by-stage breakdown). Status/stage filtering. Full approval history with comments. Version history panel per document. All endpoints district-scoped with student-district ownership validation (joins through schools table). Input validation: document type whitelist, positive integer IDs, string length limits. Role-gated: privileged staff only (admin, case_manager, bcba, sped_teacher, coordinator). Audit-logged. Migration: `011_document_approval_workflow.sql`. API: `/api/document-workflow/*`. Frontend: `/document-workflow` page under Compliance sidebar section. Route file: `artifacts/api-server/src/routes/documentWorkflow.ts`.
-   **SIS Integration (PowerSchool, Infinite Campus, Skyward, CSV):** Admin-only SIS settings page at `/sis-settings` for connecting student information systems. Provider abstraction layer with connectors for PowerSchool (OAuth2 REST), Infinite Campus (REST API), Skyward (REST API), and CSV file upload. Each connector implements `fetchStudents()`, `fetchStaff()`, and `fetchAttendance()`. Connection CRUD, test connection, manual sync trigger, and sync history log. Sync engine upserts students/staff by externalId/email, **archives unenrolled students** (marks inactive when SIS no longer reports them), logs all sync runs (added/updated/archived counts, errors, warnings) to `sis_sync_logs` table. **Nightly scheduler** checks connections at 2 AM and auto-syncs enabled connections with `syncSchedule: "nightly"`. Credentials are **encrypted at rest** using AES-256-GCM (via `SESSION_SECRET`-derived key). District-scoped connections via `districtId` FK. CSV connector supports flexible column mapping (auto-detects common header names). Setup wizard SIS step creates a real connection record. DB tables: `sis_connections` (provider, credentialsEncrypted text, districtId, schoolId, status, schedule), `sis_sync_logs` (connection, type, counts, errors/warnings, timestamps). API: `/api/sis/providers`, `/api/sis/connections` (CRUD), `/api/sis/connections/:id/test`, `/api/sis/connections/:id/sync`, `/api/sis/connections/:id/upload-csv`, `/api/sis/sync-logs`. Backend: `artifacts/api-server/src/lib/sis/` (types, powerschool, infiniteCampus, skyward, csvConnector, syncEngine, credentials, scheduler, index). Sidebar link under "Admin Tools".
-   **Agency & Contract Management:** Admin-only module for managing contracted service agencies (OT, PT, speech, ABA). Agency CRUD with contact info, linked providers (staff→agency mapping via `agency_staff` junction table with unique constraint on `(agency_id, staff_id)`), and contract management (service type, hours, rate, date range, alert threshold). **District-scoped:** all agency/contract endpoints derive the caller's district from Clerk claims via `requireDistrictId()` (fails closed with 403 if unresolvable) and filter accordingly. Staff linking validates staff belongs to same district. **Contract overlap prevention:** active contracts for the same agency + service type + overlapping date range are rejected on create/update (409 Conflict), including status-only activation. Date validation enforces `startDate <= endDate`. Session-to-contract attribution via `contract_session_links` table with unique constraint on `session_log_id` (one contract per session). `reconcileContractSessionLinks(districtId)` deletes and rebuilds links for active contracts only (preserving historical links for inactive/expired), fires before utilization/alert reads. Utilization dashboard shows consumed vs. remaining hours with visual progress bars and color-coded thresholds. Contract alerts surface when utilization exceeds configurable threshold (default 80%) or contract renewal is within 30 days. Severity escalates to critical at 95% utilization or 7 days to expiry. **Contract renewals also surfaced on admin dashboard** via `contractRenewals` field in `/dashboard/summary` response (district-scoped via server-side resolution, fails closed). DB tables: `agencies`, `agency_contracts`, `agency_staff`, `contract_session_links`. API: `/api/agencies/*` (CRUD + staff linking), `/api/contracts/utilization`, `/api/contracts/alerts`, `/api/contracts/reconcile`. Frontend: `/agencies` list page, `/agencies/:id` detail page with contracts and linked providers, `/contract-utilization` dashboard, Contract Renewals card on admin dashboard. Nav links under Admin Tools (Agencies) and District & Reports (Contract Utilization).
-   **Subscription & Tenant Billing:** District-level subscription management with Stripe integration. Three plan tiers: Starter (10 seats, $99/mo), Professional (50 seats, $299/mo), Enterprise (unlimited seats, $599/mo) — each with monthly and annual pricing. DB table: `district_subscriptions` (districtId FK, stripeCustomerId, stripeSubscriptionId, planTier, seatLimit, status, currentPeriodEnd). Stripe webhook handler registered BEFORE `express.json()` in `app.ts` for raw Buffer processing. `stripe-replit-sync` manages the `stripe` schema (products, prices, subscriptions, customers, etc.) with automatic webhook sync. Billing API: `GET /api/billing/subscription` (district's plan + seat usage), `GET /api/billing/status` (subscription gating check), `POST /api/billing/checkout` (create Stripe checkout session), `POST /api/billing/portal` (Stripe customer portal), `GET /api/billing/plans` (available plans from synced Stripe products), `GET /api/billing/tenants` (super-admin: all districts with plan status), `POST /api/billing/sync-subscription` (sync Stripe subscription state). Frontend: `/billing` page with plan overview cards, seat usage progress bar, Stripe portal link, and plan selection with monthly/yearly toggle. `/tenants` page for super-admin tenant management with summary stats and searchable district list. `SubscriptionBanner` component shows dismissible alert for past_due/canceled/unpaid subscriptions. Seed script: `scripts/src/seed-products.ts` creates idempotent plan products in Stripe. Stripe client: `artifacts/api-server/src/lib/stripeClient.ts` (Replit connector credential fetching). Nav: Billing + Tenant Management under Admin Tools.
-   **Module & Tier Gating:** Product modules (Compliance Core, Clinical & Instruction, District Operations, Engagement & Access) gated by subscription tier (Essentials, Professional, Enterprise). Districts have `tier` and `tier_override` fields. Shared config in `lib/db/src/schema/moduleTiers.ts` (backend) and `artifacts/trellis/src/lib/module-tiers.ts` (frontend). Backend middleware `tierGateByRoute` in `app.ts` auto-checks API routes against tier. Frontend `TierProvider` context fetches district tier via `/api/district-tier`. `useFeatureAccess(featureKey)` hook checks access. Sidebar shows lock icon + tier badge for gated items. `FeatureGate` wrapper component shows upgrade prompt on gated pages. Template tier logic in TemplateManager migrated to use the module system.
-   **Unified Compliance Hub:** Single `/compliance` page with three hash-based tabs: Service Minutes (default), Checklist (`#checklist`), and Timeline (`#timeline`). Old routes `/compliance/checklist` and `/compliance/timeline` redirect via HashRedirect. Sidebar consolidated to single "Compliance" nav item. ProgressRing shows "X of Y students on track" (deduplicated by student — all requirements must be on_track/completed). Checklist button relabeled from "Generate Alerts" to "Refresh Alerts". Compliance alerts auto-generated every 6 hours via `generateComplianceAlerts()` in the reminder scheduler alongside overdue contact/evaluation/transition checks. Exported from `complianceChecklist.ts` and called in `reminders.ts`. API: `/api/compliance/checklist`, `/api/compliance/checklist/run-alerts`.
-   **Student Header Enrollment Display:** Student detail page header subtitle line shows enrollment status (Enrolled/Withdrawn/Graduated/Transferred/Inactive) with date, using the correct event date for the current status. Calendar icon indicates enrollment info.
-   **Overdue Session Log Reminders (Compliance Alerting):** New scheduler check `runOverdueSessionLogCheck()` in `reminders.ts` runs every 6h alongside other reminder jobs. Looks back 2-7 days at active recurring weekly service `schedule_blocks` (skipping weekends and respecting `effectiveFrom`/`effectiveTo`), uses set-based queries (single bulk session-log fetch + single bulk alert dedupe fetch + batched insert) to find missing logs per `(staffId, studentId, date)` tuple. Creates one in-app alert per missing occurrence with severity scaled by age (medium <3d, high 3-4d, critical 5+d) and a `[ref:YYYY-MM-DD]` marker for dedupe; skips creating duplicates if an unresolved alert with the same marker already exists. Sends one digest email per provider via Resend with a 24h cooldown (cooldown checks `communication_events` filtered to `status='sent'` within last 24h). Email builder: `buildOverdueSessionLogEmail()` in `email.ts`; new `EmailType` `"overdue_session_log_reminder"`. Frontend: `/alerts` page maps `overdue_session_log` type to `/sessions` for "View Details". Compliance engine auto-resolve scope tightened — only resolves the 4 types it generates (`behind_on_minutes`, `projected_shortfall`, `missed_sessions`, `conflict`) AND preserves snoozed alerts (`snoozedUntil > NOW()`) so user-snoozed items survive re-runs.
-   **Alerts Workflow Hub:** Alerts page (`/alerts`) with three tabs (Open, Snoozed, Resolved). Features: (1) Bulk resolve — checkboxes on each card + "Select All" toggle + "Resolve Selected" button opening a single resolution note dialog for batch resolution via `POST /api/alerts/bulk-resolve`. (2) Source links — each alert card shows a "View Details" link computed from alert type and studentId (navigates to student profile, compliance page, or protective measures as appropriate). (3) Snooze — each open alert has a "Snooze" button that hides it for 7 days (`PATCH /api/alerts/:id/snooze`), setting `snoozed_until` column. Snoozed alerts excluded from Open tab and sidebar badge count. Snoozed tab shows snoozed alerts with expiry date. Severity filter pills available on Open tab.
-   **Proactive Service Minute Alerts:** Student detail page shows an amber or red alert banner below the nav bar when any service requirement is at_risk, slightly_behind, or out_of_compliance. Each at-risk service is listed with status label, service name, delivered/required minutes, and remaining deficit.
-   **Dashboard Medical Event Alerts:** Admin dashboard shows a red "Medical Events Today" banner only for same-day incidents requiring medical follow-up (911 calls, ambulance/medical attention, student injuries from restraint incidents). Static medical conditions (allergies, medications, diagnoses) live in student folders only — not on the dashboard. Links directly to student detail. District/school scoped.
-   **Dashboard Uncovered Sessions Count:** Admin dashboard "Makeup Needed" metric card now shows live uncovered sessions count for today (from coverage_instances table). Properly scoped by school/district via staff school membership.
-   **Emergency Alert Inline Cards:** `EmergencyAlertInline` component shows critical/severe medical alerts and primary emergency contact inline in forms. Displayed in: session log dialog (after student selection), quick-log-sheet (service step), and incident reporting form (step 1). Resets state on student change, checks `r.ok` before parsing. Complements the existing `StudentQuickView` popover in session list rows.
-   **Pilot Success Metrics Dashboard:** Executive dashboard (`/executive`) includes a "Pilot Success Metrics" section tracking the 5 pilot KPIs: IEP Roster Coverage (target: 100%), Session Logging within 48h (target: 80%), Incident Reporting within 24h (target: 100%), Expired IEPs (target: 0), Staff Engagement (target: 3 logins/wk). API: `GET /api/dashboard/pilot-metrics` (tier-gated to district.executive). Uses `count(distinct students.id)` for accurate IEP coverage. Staff engagement metric is placeholder (0) pending audit log aggregation.
-   **Dashboards:** Role-specific dashboards (Admin, SPED Teacher, SPED Student) provide an overview of KPIs and activities.
-   **Student & Teacher Portals:** Blackboard-style student portal for assignments and grades; teacher portal for class management and gradebooks.
-   **Analytics & Reporting:** Multi-tab analytics covering overview, behavior, academic, minutes, student deep dives, and a **Safety** tab dedicated to protective measures analysis (incident trends, antecedent breakdown, episode→PM probability ratio, student frequency drill-down, compliance indicators). 251 realistic seed incidents across 16 students spanning SY 2025-26 with phase-based trend variation. Backend endpoints: `/analytics/pm-overview`, `/analytics/pm-by-student`, `/analytics/pm-antecedents`, `/analytics/pm-episode-ratio`, `/analytics/pm-phase-trends`. Protective Measures page includes a collapsible "Incident Trends & Insights" panel with monthly sparkline, antecedent bars, and high-frequency student alerts.

## External Dependencies

-   Node.js
-   PostgreSQL
-   Vite
-   Tailwind CSS
-   shadcn/ui
-   Recharts
-   wouter
-   Express
-   Drizzle ORM
-   Zod
-   Orval
-   Clerk (for authentication)
-   Stripe (payment processing via Replit integration, in @workspace/api-server + @workspace/scripts)
-   stripe-replit-sync (webhook processing and data sync, in @workspace/api-server)
-   Resend (transactional email delivery, in @workspace/api-server; requires RESEND_API_KEY secret to enable; graceful not_configured fallback when absent)

## Session Edit History & Restore (Pilot Trust / FERPA)
-   **Audit coverage:** Every session create / bulk create / update / delete writes a row to `audit_logs` via `logAudit()` (`artifacts/api-server/src/lib/auditLog.ts`). Each entry captures actor user id, role, IP, summary, and full `oldValues` / `newValues` JSON. Updates use `diffObjects()` so the audit row only reflects fields that actually changed (a no-op edit logs as "no field changes" instead of falsely claiming a change).
-   **At-a-glance signal:** `session_logs.last_edited_by_user_id` and `last_edited_at` are stamped on every PATCH and on restore. These are denormalized for quick UI display; the full history still lives in `audit_logs`.
-   **Soft delete + restore:** Deletes set `deleted_at` (already respected by every read query). Admin-only `POST /api/sessions/:id/restore` clears `deleted_at` and writes an audit row with `metadata.restore: true`. Note: restore does **not** automatically re-credit minutes to a compensatory obligation — admins should re-edit the session afterwards so the normal PATCH flow applies the math against the current obligation state, avoiding double-counting.
-   **History endpoint:** `GET /api/sessions/:id/history` returns the row's current state plus the full ordered audit trail. Works on soft-deleted sessions too (so admins can investigate what happened to a deleted record).
-   **UI:** "View History" button in `EditSessionDialog` opens `SessionHistoryDialog` (`artifacts/trellis/src/pages/sessions/SessionHistoryDialog.tsx`). Shows session date, last-edited-by/at, deletion status, and a per-event timeline with action badge, actor, IP, and a Before/After diff table for changed fields. If the session is soft-deleted and the viewer is `admin`, a "Restore Session" button appears.
-   **Note on actor names:** `audit_logs.actor_user_id` stores Clerk user IDs. The `staff` table is not currently linked to Clerk users in the schema, so the UI shows shortened user IDs rather than fabricating a name lookup we can't actually do. If/when staff↔clerk mapping is added, the history endpoint can resolve names server-side without a UI change.

## Email Delivery & Communication Audit Trail (Task #65)
-   **Email service:** `artifacts/api-server/src/lib/email.ts` — Resend-backed email delivery with graceful fallback. When `RESEND_API_KEY` is not set, every attempt is recorded to `communication_events` with `status: "not_configured"` so the audit trail is always preserved. Builders: `buildIncidentNotificationEmail()` (603 CMR 46.04 required parent notification), `buildMissedServiceAlertEmail()`, `buildOverdueFollowupEmail()`.
-   **DB table:** `communication_events` — tracks every email attempt per student with: studentId, guardianId, staffId, channel, status (queued|sent|delivered|failed|bounced|not_configured), type, subject, bodyText, toEmail, toName, providerMessageId, sentAt, deliveredAt, failedAt, failedReason, linkedIncidentId, linkedAlertId, linkedContactId, metadata, createdAt.
-   **API routes** (require auth; engagement.parent_communication tier): `GET /api/communication-events?[studentId,startDate,endDate,status,type]`, `GET /api/communication-events/student/:studentId`, `GET /api/communication-events/summary`. All responses enriched with staffName, guardianName, studentName.
-   **Real email sends:** `POST /protective-measures/incidents/:id/send-parent-notification` now calls `sendEmail()` with Resend for email method; response includes `emailResult.{success, communicationEventId, notConfigured, error}`. Parent contacts POST (`/parent-contacts`) triggers follow-up reminder email when `contactMethod === "email"` and `followUpNeeded === "yes"`.
-   **Frontend audit log:** "Email Audit Log" tab added to the Parent Communication page (`/parent-communication`). Shows all events in a table: date, student, recipient (name + email), subject, type, status (Sent/Failed/Not Configured badges). Tab count shows total events. Lazy-loaded when tab is selected.
-   **To enable real delivery:** Add `RESEND_API_KEY` secret (from resend.com). Without it, all events are recorded as `not_configured` — no data is lost.

## Guardian / Parent Portal v1 (Task #66)
-   **Role:** `sped_parent` (hierarchy 5) added to `TrellisRole` in `permissions.ts` and `UserRole` in `role-context.tsx`. Home route: `/guardian-portal`.
-   **Auth:** `requireGuardianScope` middleware in `auth.ts` — checks `sped_parent` role, reads `tenantGuardianId` from JWT. Dev bypass: `x-demo-guardian-id` header. `requireDistrictScope` passes through `sped_parent` (no district claim required).
-   **Schema additions:** `guardian_visible` (boolean, default false), `shared_at` (timestamp), `shared_by_name` (text) added to `generated_documents` table. New `document_acknowledgments` table (`lib/db/src/schema/documentAcknowledgments.ts`): documentId, guardianId, acknowledgedAt, ipAddress, userAgent.
-   **API routes** (`artifacts/api-server/src/routes/guardianPortal.ts`, all under `/guardian-portal/*`, all protected by `requireGuardianScope`):
    - `GET /guardian-portal/me` — guardian profile + linked student
    - `GET /guardian-portal/documents` — shared documents with acknowledgment status
    - `GET /guardian-portal/documents/:id/view` — HTML snapshot for in-portal reading/print
    - `POST /guardian-portal/documents/:id/acknowledge` — idempotent acknowledgment; writes to `document_acknowledgments` and `communication_events`
    - `GET /guardian-portal/meetings` — IEP team meetings (last 3 months)
    - `GET /guardian-portal/contact-history` — parent contact log (last 50 entries)
-   **Staff share endpoint:** `PATCH /generated-documents/:id/share` — toggles `guardian_visible`; sets/clears `shared_at` and `shared_by_name`. Audit-logged.
-   **Frontend pages** (`artifacts/trellis/src/pages/guardian-portal/`):
    - `GuardianPortalHome.tsx` — overview dashboard with quick-nav cards and pending-acknowledgment count
    - `GuardianDocuments.tsx` — document list, inline HTML viewer (sandboxed iframe), print/save PDF button, acknowledge button
    - `GuardianMeetings.tsx` — upcoming + past meetings with type/status/location
    - `GuardianContactHistory.tsx` — contact log with method icons and outcome text
-   **Navigation:** `guardianPortalNav` + `sped_parent` config added to `roleConfig` in `AppLayout.tsx`.
-   **Routing:** `GuardianPortalRouter` added to `App.tsx`, rendered when `role === "sped_parent"`.

## Generated Document Pipeline (Task #64)
-   **DB table:** `generated_documents` — stores rendered HTML snapshots per student with fields: id, studentId, type (incident_report|progress_report|iep_draft), status (draft|finalized|archived), title, htmlSnapshot (full HTML for re-print), linkedRecordId, createdByName, createdAt, updatedAt. Indexes on studentId, type, linkedRecordId.
-   **API routes** (all require auth; roles: admin, case_manager, bcba, sped_teacher, coordinator, provider): `GET /api/generated-documents?studentId=X[&type=Y]`, `GET /api/generated-documents/:id` (includes htmlSnapshot), `POST /api/generated-documents`, `PATCH /api/generated-documents/:id` (status, title). District scope enforced via student→school→district join. All mutations audit-logged.
-   **Shared print utility:** `artifacts/trellis/src/lib/print-document.ts` — `buildDocumentHtml(opts)` builds a consistent branded HTML template (Trellis emerald header, meta grid, labeled sections, signature lines, footer); `buildIncidentReportHtml(opts)` builds the full 603 CMR 46.00 incident report; `openPrintWindow(html)` opens a new tab and triggers browser print dialog; `saveGeneratedDocument(params)` fire-and-forgets a POST to the API to persist the snapshot.
-   **Incident Report (protective-measures.tsx):** "Preview Restraint Report PDF" / "Download Restraint Report PDF" buttons now call the client-side generator. Staff map is built from the `staff` array prop; incident fields are mapped to seven labeled sections (Incident Info, Behavior & Context, Restraint Details, Injuries & Medical, Parent Notification, Follow-Up & Debrief, Administrative Review). The rendered HTML is immediately opened for printing and concurrently saved to generated_documents.
-   **Progress Report (student-iep.tsx):** Existing `printReport()` enhanced to save a document record after printing.
-   **IEP Builder Draft (iep-builder.tsx):** Existing `printDraft()` enhanced to save a draft record after printing.
-   **Generated Docs tab:** New "Generated Docs" tab added to the student IEP page (`/students/:id/iep`). `GeneratedDocsPanel` component fetches all generated documents for the student, displays title/type/status/date/creator, supports re-print by fetching the stored htmlSnapshot and opening a print window, status updates (draft→finalized, archive), and skeleton loading/empty state.

## Security Hardening (Task #62)
-   **Tenant Isolation:** `AuthedRequest` carries `tenantDistrictId`/`tenantStaffId`/`tenantStudentId` from Clerk token metadata; helpers `getEnforcedDistrictId()` and `enforceDistrictScope()` in `artifacts/api-server/src/middlewares/auth.ts`
-   **`enforceDistrictScope` middleware:** Applied globally at `app.use("/api", enforceDistrictScope)` (after `requireActiveSubscription`, before router). In production, overwrites `req.query.districtId` from the auth token and removes `schoolId` — prevents tenants from crossing district boundaries via crafted query strings. No-ops in development.
-   **Role switcher:** Gated on `import.meta.env.DEV` (frontend) and `process.env.NODE_ENV !== "production"` (backend `/api/demo/switch-role`).
-   **Path-scoped role guards (routes/index.ts):** `router.use("/students", requireStaffOnly)`, `router.use("/sessions", requireStaffOnly)`, `router.use("/staff", requireStaffOnly)`, `router.use("/reports", requirePrivilegedStaffOnly)`, `router.use("/protective-measures", requirePrivilegedStaffOnly)` — blocks sped_student from all staff-facing resources without leaking middleware across unrelated sub-routers (avoids the Express sub-router bleed-through problem).
-   **Permission matrix test suite:** `tests/permission-matrix.mjs` — 89 checks across 14 categories (unauthenticated, students, sessions, staff, workload-summary, uncovered-sessions, schedule-blocks, incidents, iep-goals, reports, report-exports, audit-logs, student-portal). Run with `node tests/permission-matrix.mjs`. Uses `x-test-user-id` + `x-test-role` + `x-test-district-id` headers (accepted in non-production mode only).
## Compliance Trends Page (Apr 2026)
-   **New page:** `/compliance/trends` (`artifacts/trellis/src/pages/compliance-trends.tsx`) — unified time-series view of four compliance metrics, replacing the single-metric `ComplianceTrendTab`. Sidebar nav: Compliance → "Compliance Trends". Window selector: 6 / 12 / 18 / 24 months.
-   **New endpoint:** `GET /api/dashboard/compliance-trends?months=N` (`artifacts/api-server/src/routes/dashboard/complianceTrends.ts`) returns four monthly series in one payload:
    1. `serviceMinutes` — required vs delivered minutes per month (interval-normalized: weekly×4, quarterly÷3) plus a compliance %.
    2. `atRiskStudents` — count of tracked students who received <70% of their monthly required minutes.
    3. `compensatoryExposure` — accrued vs delivered comp minutes per month, plus cumulative open balance seeded from pre-window net (so trends don't falsely start at zero).
    4. `loggingCompletion` — % of completed/missed/makeup sessions logged within 48h of session date (the documented pilot timeliness KPI).
-   **Date logic:** All month math is done in UTC via string manipulation (e.g. `axis = ["2025-05", …, "2026-04"]`) to keep boundaries consistent with how Postgres timestamptz returns ISO. `endStr` is the UTC last-day-of-month derived from `Date.UTC(y, m+1, 0)`. Mixing local-time `Date` constructors with `toISOString()` was causing off-by-one cutoffs in non-UTC deployments.
-   **Per-month effective requirements:** Required minutes are recomputed for *each* axis month from `service_requirements.start_date` / `end_date` intersection, NOT projected from today's active set. Avoids retroactive shifts when requirements are added/edited mid-year.
-   **Sparseness honesty:** Months with zero applicable requirements return `compliancePercent: null` (and `atRiskCount: null`); months with zero terminal-status sessions return `timelinessPercent: null`. The UI renders these as gaps in lines (no `connectNulls`) rather than fabricated 0%/100% values. A `dataQuality: "ok" | "sparse" | "empty"` flag drives an amber banner when fewer than 1/3 of months in the window have any activity.
-   **Soft-delete safe:** `session_logs.deleted_at IS NOT NULL` rows are excluded from all four series.
-   **Limitations surfaced in UI footer:** comp accrual month uses `compensatory_obligations.created_at` (when recorded, not when underlying service was missed); compliance % can shift retroactively if a requirement's `requiredMinutes` is edited (recomputed on read).
-   **Auth:** Inherits district scope via `getEnforcedDistrictId(req)` and the standard `/dashboard/*` mount (`requireAuth` + `requireDistrictScope`).
